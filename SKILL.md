---
name: tiku-search
description: "检索结构力学题库：按图片或荷载搜相似题目，并按排名取答案"
user-invocable: true
---

# 结构力学题库检索

这个项目用于维护和检索结构力学题库。题库索引保存在章节 Excel 中，第一列是题目图片相对路径，第二列是荷载 JSON。

当前脚本入口以项目根目录 `F:\cc\7-题库检索` 为准。旧文档里如果出现 `D:\桌面\答疑、帮做\结构力学\帮做\search.py` 之类路径，那是题库数据目录或历史脚本路径，不是当前仓库脚本入口；运行命令时应在本项目根目录执行 `python search.py ...` 或 `python scripts\...`。

CLI 检索默认在终端输出候选并写入 `_last_search.json`，不会自动打开匹配题图。候选路径可直接查看；需要取答案时使用 `answer` 命令。`answer` 会把答案复制到配置的 `answer_output` 目录，调用前确认该目录配置正确。

## 常用命令

按荷载描述检索：

```powershell
python scripts/search_by_loads.py loads-search --types "均布" --raws "20kN/m" --chapter "2静定结构"
```

荷载 raw 统一不带物理单位；`type` 决定量纲。手动输入可以带单位也可以不带单位，程序会统一归一为无单位数字：`5kN`、`5kN/m`、`5kN·m` 都按对应 `type` 存为 `5`。

多个荷载用空格一一对应：

```powershell
python scripts/search_by_loads.py loads-search --types "集中" "弯矩" --raws "10kN" "10kN·m" --chapter "2静定结构"
```

按图片检索并启用 LLM 复筛：

```powershell
python scripts/search_by_loads.py image-search --image "D:\path\to\question.jpg" --chapter "2静定结构" --rerank
```

实验多 Agent 检索（Qwen 识别分类 + 规则路由主库/字母库 + 可配置视觉复筛）：

```powershell
python scripts/multi_agent_search.py --image "D:\path\to\question.jpg" --chapter "2静定结构"
```

临时使用 Qwen 视觉复筛（不改变配置文件）：

```powershell
python scripts/multi_agent_search.py --image "D:\path\to\question.jpg" --chapter "2静定结构" --rerank-provider qwen --rerank-model qwen3.7-plus
```

对照 Prompt 和提供方（会向选定的外部模型发送题图，运行前需确认 API 费用与数据范围）：

```powershell
python scripts/evaluate_rerank_matrix.py --prompts v1 v4 --providers zhipu qwen --workers 10
```

多 Agent CLI 默认对字母库大候选池启用 V5.2 尺寸复筛；如需临时回退：

```powershell
python scripts/multi_agent_search.py --image "D:\path\to\question.jpg" --chapter "2静定结构" --disable-dimension-filter
```

图片检索可让 Qwen 根据题干文字自动建议章节。当前先独立提取 `visible_problem_text`：没有实际题干时直接返回 `needs_chapter`，有题干时才采用非 `unknown` 且置信度不低于 `0.45` 的判断；2/3 章允许“桁架指定杆轴力”“静定结构位移/图乘法/单位荷载法/EA 为常数”等典型题型文字并结合结构信息推断章节：

```powershell
python scripts/multi_agent_search.py --image "D:\path\to\question.jpg" --chapter auto
```

不调用模型、只验证路由和检索：

```powershell
python scripts/multi_agent_search.py --types "均布" --raws "q" --chapter "2静定结构" --no-rerank
```

启动从 Git 历史恢复的独立桌面 GUI（不替代网页 Agent 或飞书服务）：

```powershell
python legacy_gui.py
```

桌面 GUI 的单题入库遵守 plan -> confirm -> execute，并在仓库外备份；“漏存审查”只生成只读计划和报告，不直接批量写入 live 题库。

按上一次检索排名取答案：

```powershell
python scripts/search_by_loads.py answer 1
```

核心 CLI 的等价命令是：

```powershell
python search.py answer --rank 1
```

题库飞书机器人 dry-run 状态机测试：

```powershell
python scripts/feishu_tiku_bot.py dry-run-flow --image "D:\path\to\question.jpg" --chapter 5 --choice 1
```

启动题库飞书机器人和临时公网隧道：

```powershell
.\启动结构力学题库.bat
```

临时飞书事件 URL 会写到 `.tmp_feishu_tiku\feishu_tiku_latest_url.txt`。

## 当前检索流程

- 荷载粗筛用于建立有限视觉复筛池：有 100% 匹配时保留全部满分候选，满分不足 3 个时用下一档候选补齐；没有 100% 匹配时取粗筛前 3 个。复筛未执行、失败或回退粗筛时仍保留原粗筛顺序。
- Excel、飞书、CLI 和 Agent 的荷载 raw 统一使用无单位格式；单位只由 `type` 表示，检索前会把 `kN`、`kN/m`、`kN·m`、`kN.m` 等单位去掉。模型识别时也要求按箭头/力偶形态判断类型，不根据单位判断类型。
- CLI 图片检索需要显式添加 `--rerank` 才启用复筛。
- 实验多 Agent CLI 支持 `--chapter auto`。自动章节只在 Qwen 输出非 `unknown`、置信度不低于 `0.45` 时使用；4/5/6/7/8 章仍主要依赖明确方法词或强方法步骤证据，纯结构图、荷载、支座、尺寸、EI 不能自动猜章节。
- 复筛池中的候选不因数量少而跳过；有限复筛池最多 3 个，候选分数只影响排序，不在视觉层提前删除。
- 复筛 Prompt 应比较候选图和查询图的结构拓扑、支座连接以及荷载位置/方向/分布；尺寸、文字、节点编号和题号不作为主要差异。当前生产使用 V1 shape-only Prompt，粗筛荷载分仍以独立 50% 权重参与最终分；V4 load-aware Prompt 已完成首轮对照，但同类召回不足，暂不切换。
- 复筛提供方由 `rerank_provider` 配置为 `zhipu` 或 `qwen`；共享 8788/8790/CLI 默认仍是智谱 `glm-4.6v`，隔离 8896 A2 使用评测胜出的 Qwen `qwen3.7-plus` + V1 Prompt。首轮单候选 8 秒超时后，会按粗筛分从高到低最多单独补评 3 个，每个补评 10 秒；若仍有候选未完成，整次结果回退为粗筛排序并提示“复筛未完成”，不会混合未验证视觉分和最终排序。
- 如果复筛后最终相似度 100% 的候选超过 1 个，会额外按杆件长度、跨长、高度和整体比例做打平复核。
- 复筛后的用户可见结果只显示最终相似度。
- 复筛结果保留 `>=90%` 的全部候选；没有候选达到 90% 时，按排序最多展示 Top 3 个达到暂定可靠门槛的候选；最高分低于可靠门槛时返回“未找到可靠相似题”。这个规则只用于正常完成的复筛结果，不改变复筛池；复筛超时、失败或不完整时仍回退粗筛排序。`_last_search.json` 会按最终显示排序重写，低于可靠门槛时会清空，避免旧排名被继续取答案。
- 检索结果路径会自动解析：如果 Excel 里的 `题目名称` 指向的图片不存在或大小写不一致，程序会先在同章节/最近的题目目录下递归查找同名图片；只有候选唯一时才自动更新 live Excel 路径，并删除同路径同荷载的重复行。
- 自动更新 live Excel 前会在项目 `backups/auto_path_repair_时间戳/` 下备份本次进程触碰到的章节 Excel。
- CLI、飞书和 Agent 共享同一套路径解析逻辑；图片移动到新子文件夹后，检索时可自动恢复。
- 多 Agent 流程由 `QwenClassifier` 负责荷载识别和分类，`RuleRouter` 决定查主库、字母库或进入复核区，候选图视觉复筛由 `rerank_provider` 选择 Zhipu 或 Qwen。
- 多 Agent 复筛候选池按库区分阈值：主库非满分候选需 `>=65%`，字母库非满分候选需 `>=50%`；两类库的 `100%` 候选都全部进入候选池，先保召回。达到阈值的候选不因数量少而跳过复筛。
- 字母库结构类型筛选优先使用题干文字里已经明确出现的结构类型，例如“静定梁 / 静定钢架 / 桁架 / 拱”；只有文字没有明确类型时，才额外调用结构图视觉分类，减少一次模型看图耗时。飞书新增题目写入字母库时也必须同步写入 `结构类型`，否则新题会被后续结构类型筛选漏掉。
- 尺寸层在 8790、8788、8793、8794 和多 Agent CLI 默认开启，只在 `route=symbolic`、上游已知结构类型、荷载粗筛 100% 候选 `>20` 时触发，不重复识别结构类型，只看查询图一次并缓存结果。完整尺寸与完整尺寸明确不同才硬删；单边尺寸与完整尺寸任一边或另一个单边相同时提前，不同时仍保留。拱不参与；模型失败、query 未校验、candidate 缺尺寸或全删空时都回退原候选池。命令行入口可用 `--disable-dimension-filter` 临时回退。
- 8790 是网页主线和邀请制内测入口；公网模式使用有界任务队列、同会话串行、每日估算费用硬上限、后台过期会话清理和媒体 `no-store`。网页会在上传前把手机大图最长边限制为 2560px，并用 JPEG 自适应压缩到约 1MB；必要时再降至 2048px，减少移动网络上传超时，同时保留题干、荷载符号和尺寸细节。Qwen 与智谱共享模型输入编码器，8788 飞书、8790、8794 和图片 CLI 绕过网页传入的大图也会在内存中执行同一压缩策略；小图原样发送，用户文件和题库原图不得改写。8790/8794 的 `PARTIAL` 有可用结果时必须显示降级提示；正式错误（包括 HTTP、流式解析和业务 `ERROR`）必须区别于成功消息、可点赞/点踩并给出下一步。等待消息不可评价。8788/CLI 不增加网页交互，8793 按用户要求继续保留原稳定演示/回退快照；未经明确验收不要刷新。公网开放前必须显式设置 `--daily-budget-cny`，并在外层使用独立 Cloudflare Tunnel 与 Access 白名单；不要复用 8788 飞书隧道。
- 8891 是隔离权威预检 MVP：千问预检后由代码确认 A1/A2/A3；A1/A3 把预检观察和线路边界交给第二次千问生成用户说明，不调用检索；A2 跳过旧多题判断后继续现有精确识别、章节确认、主库/字母库路由、粗筛、尺寸筛选和视觉复筛。8891 关闭旧并行外荷载检查，使用独立 Cookie 和运行目录，不改变 8890 影子线路或 8790 正式线路。
- 8896 在 A2 与 A3→A2 共用严格章节三态：七类支持题映射到固定存储键，动力学等明确超范围题停止，公式或证据不足才进入 `WAIT_CHAPTER`，非中文题干要求上传含中文版本。询问态先走代码目录，模糊自然表达再走 Intent V2，模型结果仍由代码校验；纯寒暄保留当前题，只有已提供全局搜索选项且用户明确同意时才执行。8896 A2 视觉复筛独立使用 8 路首轮、12 秒超时和最多 8 个低并发重试；共享 8790、8794、8788 和 CLI 默认策略不变。该开关不启用在 8790、8794、8788 或 CLI。
- 8892 是 A3 Phase 2 隔离离线入口：只执行结构化分组、图角色判断、独立题目组章节提示、OpenCV 裁剪、质量校验和 `no_unit/single_ready/multiple_wait_choice/uncertain` 状态判定，不调用 A2 或题库。`scripts/map_a3_regions.py` 先识别题目分组和粗区域；`scripts/crop_a3_regions.py` 可在已保存区域内离线联合有效前景分量并用相邻区域限制外扩，不调用模型。该精裁实验能保留断开的荷载、尺寸和支座，但密集公共题干及跨区手写标记仍可能混入相邻标签，因此当前裁图不能直接标为 `single_ready` 或交给 A2。拆题 Prompt 不判断章节；章节识别共享现有章节规则，同组只调用一次，不同组最多两路并发。一个可靠单元将在后续阶段直接交给 A2；多个可靠单元必须先让用户选择。A3 章节仅作逐单元提示，不能用现有 `chapter_override` 静默覆盖 A2。
- `scripts/inspect_paddle_layout_candidates.py` 只读取已保存的 PP-Structure 布局 JSON，给 `image` 框固定候选 ID，标记近重复、整组大框、单容器和贴页边框，并导出真实裁片、叠框图和清单。它不调用 Paddle、模型、A2 或题库；几何框不能证明荷载、尺寸和支座完整，所以输出保持 `review_required`，不得直接交给 A2。
- 普通请求失败保留原输入并提供“重试上一条”；业务 `ERROR` 保留题图时提供“重试搜索”。认证、上传、断网和浏览器历史异常分别提供适用的重新登录、重新上传或重新连接动作。上传新题或切换 A3 子题只清当前状态，已显示媒体保留到新建对话或会话过期；媒体确实失效时只原地提示并禁用对应候选，不另发错误消息。A3 裁剪通过后先显示用户裁剪图，再显示 A2 回复。额度详情不得被通用 5xx 覆盖。临时会话自然过期时直接返回初始页，不生成错误卡或评价入口。Cookie 失效跳回邀请码页必须解释重新登录原因，但登录页不放评价按钮。反馈提交失败保留弹窗内容和原因选择，允许原地重试；反馈通道自身不递归评价。
- 8795 是独立管理员后台，使用独立管理员 Cookie 和 `.tmp_tiku_admin_8795/control.sqlite3`，读取 8790 的费用与反馈数据但不与 8790 共用 HTTP 端口。后台支持邀请码生命周期和动态额度、今日汇总、按日期/原因/邀请码/章节/处理状态筛选反馈、查看完整可见对话与单题估算费用。邀请码明文只在创建或重置时显示一次；后台本机验收完成后再绑定受 Cloudflare Access 保护的独立 `admin` 子域名。
- 8790 只有在显式使用 `--control-db` 后才会动态读取 8795 的邀请码和预算设置。迁移前先用 `scripts/manage_tiku_admin.py --import-invites` 做只读预检，确认无 ID/哈希冲突后再显式添加 `--apply-import`；合并会保留后台新邀请码、补入旧邀请码并迁移旧 Cookie 签名密钥，重复执行不会重复创建。之后仍须在维护窗口受控重启 8790；`--control-db` 不得与 `--invite-config` 同时使用，不要为了上线后台擅自重启当前 8790。
- 8790 正式切换使用 `scripts/switch_tiku_agent_8790_control.ps1`。不传 `-Apply` 时只检查 8790/8795 健康、控制库冲突和当前看门狗唯一性；用户明确确认维护窗口后才传 `-Apply`。脚本会做仓库外一致性备份、精确停止已验证进程、以控制库模式启动，并用临时邀请码验证登录和动态停用；失败时自动恢复旧哈希配置模式。
- `scripts/feishu_tiku_bot.py` 是本项目原生的专职题库飞书机器人 MVP。第二大脑项目只作为飞书接入参考，不直接复用其业务代码。当前已支持 dry-run 状态机：收图、问章节/自动章节、返回候选、选择答案；真实飞书图片下载/上传封装在 `FeishuClient` 中。
- 独立 Agent 的 Intent V2 在普通章节候选页支持“没有/都不是”整批否定、“继续搜/换一批”继续检索、“回到候选”和“答案不对”。继续检索只沿用当前题图、章节、路由和结构类型，排除已尝试过的粗筛候选后取下一批；候选耗尽后不再调用工具。`retry_search` 仍只用于 `ERROR` 状态，不得与结果不匹配混用。8790、8793、8794 Agent 对“支持哪些章节”共同复用同一代码事实：从 `CHAPTERS` 去掉本地目录章号后生成知识范围固定答案，并说明矩阵位移和影响线仅支持含具体外荷载的题目；禁止调用模型、工具或修改会话状态，混有搜题指令时仍走各自原业务状态机。8788 飞书保持纯搜题工作流，不接入寒暄或固定事实问答。参数式 CLI 没有自然语言对话入口，不额外增加伪问答命令。该能力不自动跨章节或降低复筛阈值。
- 飞书机器人默认使用自动章节模式：收到图片后先尝试 `chapter=auto`，识别成功直接检索；识别不到再让用户回复章节号。用户可发送 `a` 在自动/手动章节模式之间来回切换，切换后重新发送题图生效；也可用 `手动`/`m` 明确切手动、`自动`/`auto` 明确切自动。
- 飞书自动章节模式下会先判断题图是单题、多题还是不确定。`single`/`uncertain` 继续走原单题流程；`multi` 进入多题会话，先只返回题号、荷载和章节摘要，不立刻批量检索。用户回复题号按识别章节检索，回复 `题号-章节名` 指定章节检索；候选出来后才用 `题号-序号` 取答案。多题会话会在收图阶段用 OpenCV 预裁结构图块并按题号顺序绑定；用户查某题时若有对应裁图，则用配置的视觉复筛提供方，否则退回荷载粗筛。候选阶段 `0` 返回多题列表，列表阶段 `0` 结束本次多题会话。
- 飞书候选页支持删除错题：单题候选页回复 `-1` 到当前候选数删除对应候选；多题需要先进入某一题候选页，再回复对应负数删除当前题候选。删除前必须二次确认，回复 `1` 执行、`0` 取消；执行时会先备份 Excel，再把题目/答案图片移动到 `backups/delete_question_时间戳/`，最后从主库或字母库 Excel 中删除精确匹配的题目行。
- 飞书收到图片消息并拿到 `image_key` 后会立即给原消息添加 `OK` reaction 表情，避免长时间模型识别时用户误以为机器人没响应；可用 `--working-reaction` 调整或关闭。
- `启动结构力学题库.bat` 会启动 `scripts/start_tiku_bot.ps1`，再由 `scripts/tiku_bot_watchdog.ps1` 保活本地 8788 服务；生产回调复用系统中已安装的固定 Cloudflare Tunnel，地址为 `https://feishu-tiku.cysbuting.cn/feishu/events`，不会再启动 `trycloudflare.com` 临时隧道。不要和第二大脑的 8787 小柴服务混用，也不要影响 8790 网页 Agent。

## 注意事项

- 不要跨章节自动搜索；章节必须由用户指定、确认，或由 `chapter=auto` 在题干/题型文字证据下自动确定。现有飞书和普通 Agent 检索都不能自动遍历所有章节。Intent V2 只有一个受限例外：自动识别失败并进入 `WAIT_CHAPTER` 后，先向用户提供全局搜索选项；只有用户明确同意才执行一次只读 `global_search`。该动作收集第 2–8 章所有 `score >= 0.999` 的内容去重候选；字母库先按结构类型过滤，去重后候选严格超过 20 条时走同一套 V5.2 尺寸复筛，再以最多 10 路并发完成剩余视觉复筛。最后按“粗筛荷载分 50% + 视觉复筛分 50%”计算最终分，只返回最终分 `>= 0.95` 的候选及来源章节；不得降低阈值或自动取答案，仍须等用户按 `candidate_rank` 选择。
- 自动章节判断里先检查独立抄取的 `visible_problem_text`；为空时不看结构长相，直接返回 `unknown`。有实际题干后，“静定 + 内力图/弯矩图/剪力图/轴力图”应判为 `2静定结构`，不要限定后面是梁、钢架、多跨梁还是其他结构；桁架指定杆轴力也可判为 `2静定结构`，且不依赖模型是否给题干加引号。
- 2026-07-05 起飞书章节日志改为全量记录章节判断到 `data/feishu_chapter_failure_log.jsonl`，包括自动采用、需要手动和手动补章节样本。这个文件名沿用旧失败日志名，但内容已不再只限失败样本。
- 多题图的 LLM bbox 只能作粗区域，不能直接用于复筛；区域内 OpenCV 也必须保留断开的荷载、尺寸和支座，并经过相邻内容与完整性校验。裁图缺失或边界不可靠时不得进入 A2。章节未知或不在 2-8 章范围内的题不要自动检索，等用户指定章节或单独发图。
- 不要直接读写 `config.json`、`config.local.json` 里的密钥；需要结构时看 `config.example.json`。
- 运行前后可用 `python scripts/smoke_test.py` 做只读检查；它会全表检查荷载 JSON、图片路径存在性、路径大小写，并验证旧路径自修复案例。
- 当前主库是数值题和已赋值符号题库。独立字母库位于 live 主库旁边的 `帮做_字母库`，章节 Excel 名称和主库一致，列为 `题目名称`、`荷载`、`结构类型`。
- 字母库的 `raw` 写入相似度编码，`original_raw` 保留原始字母标注。例如 `2P/a` 写为 `{"type":"均布","raw":"0.021","original_raw":"2P/a"}`；相似度只使用 `type` 和 `raw`，不会读取 `original_raw`。
- 维护字母库/主库拆分时，先运行分类脚本导出 review，再备份 live Excel，最后用应用脚本写回。不要直接按“是否含字母”删除。
- 字母荷载第一版按三个量纲体系归一化，不把字母固定死：均布力主题、集中力主题、集中弯矩主题。长度符号也不固定为 `L`，`a/b/l` 等字母都可作为长度占位；同一个字母可根据荷载类型和表达式形态归入不同体系，例如 `A/长度、A、A*长度` 可表示集中力主题，`A、A*长度、A*长度²` 可表示均布力主题，`A/长度²、A/长度、A` 可表示集中弯矩主题；同体系按倍数映射为保留小数编码后继续走原相似度算法。
- 字母荷载相似度会做同题体系冲突消解：如果同一道题内某个主题体系明显占主导，孤立冲突项仅在相似度内部归入主导体系；不会改写原始 `raw`。
- 如果题干文字或图片说明里给出符号赋值，例如 `P=40kN`、`q=20kN/m`、`F1=40kN`、`M=20kN·m`，识别结果的 `raw` 应保留赋值关系但去掉单位，例如 `P=40`、`q=20`、`F1=40`、`M=20`。不要把这类题只当成纯字母荷载。
