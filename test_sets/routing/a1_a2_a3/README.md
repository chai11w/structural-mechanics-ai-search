# A1 / A2 / A3 Routing Baseline

This fixed regression set is grouped by the expected route:

- `A1`: out of scope or not searchable;
- `A2`: one complete searchable question;
- `A3`: multi-question or page-level decomposition required.

The source set was copied from `D:\桌面\新建文件夹` on 2026-08-23. Keep the
three category folders and do not rename images without updating the evaluation
record. `manifest.json` locks every expected route and source SHA-256. A routing
change is not promoted until this suite is compared with the
previous result and every regression is explained or rolled back.
