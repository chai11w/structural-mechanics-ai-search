# Checkpoint 优化阶段 4.5

本轮编号由用户于 2026-09-08 确认。此前 `checkpoint_phase4_5_runbook.md` 是上一轮的诊断与验收记录，保留原意；本文件管理新一轮优化的 4.5.1～4.5.5。

## 目标和范围

保留排障所需的阶段证据，同时使业务请求不等待证据存储。当前优化在 `codex/phase4-checkpoint-contract-v1` 独立 worktree 开发，不切换已有服务，不操作 live 题库，不启用生产采集。

已验证的瓶颈：同步证据采集在业务执行槽位和会话锁内，前驱审计、图片存储和 Checkpoint 写入可能逐次等待。隔离的一张答案请求在持续锁库时耗时 16.55 秒，关闭采集约 0.01 秒。SQLite 短超时不能约束 Python 锁或文件 I/O。合成数据实验用于复现，不是生产性能统计。

## 分步交付

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 4.5.1 | 冻结目标、数据语义、失败边界及验收矩阵 | 完成 |
| 4.5.2 | 题库引用、不可变阶段输入、裁图版本隔离及诊断兼容 | 完成 |
| 4.5.3 | 有界队列、固定消费者、请求侧移除采集专用 I/O | 完成 |
| 4.5.4 | 后台等待预算、维护协调、熔断、健康与有限停机 | 未开始 |
| 4.5.5 | 隔离场景验收、实际图片压力测试和交付说明 | 未开始 |

每步使用可回退提交。完成前两步不代表同步采集延迟已经解决；正式合并、部署和启用单独决定。

## 数据语义

- 用户原图和每版裁图使用独立 Artifact，保留现有 owner、TTL、校验和审计。
- 候选题使用题库引用，另存当时的候选 ID、分数、排名、可见性。
- 答案使用有顺序的题库引用，记录当时选择的候选和 candidate generation。不再为 Checkpoint 复制答案图片；业务交付所需的图片复制不属于本轮删除范围。
- 题库引用由题库标识、章节和规范化相对定位组成，通过服务端配置映射根目录。禁止记录会话临时答案路径、绝对路径、URL 或可越界的定位。
- 引用语义是“题库当前文件”，不是历史字节快照。答案以后修正或删除，历史引用将看到当前版本或不可用；本轮不引入历史题库版本库。
- 证据清理只能删除引用，不能删除题库文件。诊断读取必须先经过 Checkpoint 的权限、TTL 和审计检查，再通过受控题库根解析文件。
- 新引用记录采用可区分的结果版本，保留旧 Artifact 记录读取。旧读取器必须明确拒绝不支持的新记录，不能把引用误当成已复制的图片。

## 阶段输入和文件生命周期

- 在接触 Store、图片或其他可能等待的操作前，冻结本次阶段需要的数据及身份；不持有 live state、可变 ToolResult 或模型客户端作为待处理内容。
- A3 重新裁剪必须创建新文件名，不能覆盖前一版。后续阶段使用显式选中的资源版本，而不是根据 unit 推测“最新路径”。
- 文件删除导致证据不可用时明确降级，不读取另一版文件补位。后续队列需要有限资源保留或租约；4.5.2 不承诺进程崩溃后仍可执行未提交采集。
- 输入副本有字节、层数和条目上限，拒绝超限采集不能影响业务。输入不进入公共协议、Trace 或未受控日志。

## 异步与故障边界

- 4.5.3 区分 accepted/queued 和 stored。只有实际提交才可以发布 `checkpoint_id` 的 Trace 关联。
- 队列同时限制条数、字节数和任务年龄；队列满立即拒绝，禁止同步回退、每请求新建线程或无界重试。
- 固定消费者按完整 owner/revision/unit 协调最近已提交前驱和最近成功前驱。丢弃节点不能留下指向未提交 ID 的因果链。
- A3 父流程与嵌套 A2 共用同一请求的采集预算；预算只统计采集开销，不消耗正常模型等待时间，不在每个阶段重置。
- 不绕过 Store 的容量、TTL、完整性和查看审计。4.5.4 把大批清理留给有边界的维护流程，保留备份、plan/hash/漂移检测和管理确认规则。
- 健康状态区分成功、积压、超龄、丢弃、资源缺失和失败。停机排空有明确上限，不无限 join。Python 线程无法安全强杀，不能宣称线程超时取消了已经运行的 I/O。

## 验收矩阵

| 编号 | 场景与可观测断言 | 阶段 |
| --- | --- | --- |
| D1 | 候选与多答案引用能经章节和配置定位；采集不读取答案字节、不创建答案 Artifact | 4.5.2 |
| D2 | 绝对路径、父目录、盘符相对路径、UNC、链接、越界和非法文件类型拒绝；缺失不回退到其他文件 | 4.5.2 |
| D3 | 历史引用读取当前内容；删除或过期 Checkpoint 不影响题库原文件；旧 Artifact 证据仍可读取 | 4.5.2 |
| D4 | 创建输入后修改原 state、工具结果、候选和嵌套字段，冻结内容不变；超限数据安全拒绝 | 4.5.2 |
| D5 | 同 unit 连续裁剪路径不同，第一版字节保留；新旧阶段绑定各自几何与图片 | 4.5.2 |
| Q1 | 用 Event 阻塞 Checkpoint 写事务、Store 锁、Trace 路径锁和文件 I/O，未释放阻塞前业务已返回 | 4.5.3 |
| Q2 | 队列满、字节满和超龄时明确丢弃，线程数与内存受限，无同步回退 | 4.5.3 |
| Q3 | 快速选题、查看答案和并发乱序不串 owner/unit/revision；Trace 不出现未提交 ID | 4.5.3 |
| F1 | 持续锁库、消费者故障、容量不足和维护竞争时健康可解释，不重复模型调用或计费 | 4.5.4 |
| F2 | 阻塞消费者时停机仍在规定期限结束，剩余任务统计准确；启动与关闭开关保持兼容 | 4.5.4 |
| A1 | 开关两侧的公共结果、排名、费用、Task State、Response 一致，证据变化不影响动作授权 | 4.5.5 |
| A2 | 真实常见/较大图片，1/3/10 unit，多答案与连续操作；测采集额外 P95/P99、积压和丢失率 | 4.5.5 |

Q1 使用确定性的先后关系断言，不只依靠容易波动的毫秒断言。初始性能目标为请求侧每阶段提交 P99 10ms、每请求额外采集开销 P99 100ms，最终在隔离目标机器校准，不作为操作系统硬实时保证。4.5.2 完成时不要求 Q/F/A 矩阵已通过。

## 入口一致性

本轮影响 Agent 采集和诊断 CLI。搜索 CLI、旧飞书机器人和题库入库逻辑不改排名或交付行为。每步核对 Agent/launcher、诊断 CLI、相关 runbook 和项目 Skill 文档；只有行为接口发生变化的入口需要同步修改。项目记忆中的旧阶段进度不能代替本表。

## 4.5.1 / 4.5.2 交付验收（2026-09-08）

4.5.1 已通过本文件冻结范围和矩阵，本地提交 `f0e8f72`。4.5.2 的实现与新增验收位于 `tests/test_checkpoint_optimization.py`，并更新已有 A2/A3 集成断言。

| 验收 | 已验证结果 |
| --- | --- |
| D1 | 粗筛/复筛保存含章节的候选引用；多答案按源列表顺序引用，答案采集无需图片读取或 Artifact 写入；缺少原始引用不使用交付副本补位 |
| D2 | 非法相对定位、越界及不存在文件拒绝；真实 Windows junction 拒绝；读取前执行 scope、TTL、审计检查 |
| D3 | 修改答案文件后读取新字节；旧 schema 1 原图 Artifact 仍可读；新 schema 2 经 Store 往返；删除和过期引用不删除题库文件 |
| D4 | 嵌套候选/工具结果/输入副本与原对象隔离；首次前驱 I/O 中修改 state 和结果不影响本阶段；结构/字节超限采集拒绝但业务搜索完成 |
| D5 | 同 unit 两次裁图路径不同，旧字节保留；在前驱 I/O 中替换 live 路径/几何后，旧阶段仍绑定旧图片和像素边界，新阶段绑定新版本 |

验证命令：

```powershell
python -B -m unittest discover -s tests -q
python -B -m unittest -q tests.test_checkpoint_optimization
git diff --check
```

完整测试集 1430 项通过，约 71 秒；最后补强裁图断言后另跑优化验收集，12 项通过。用临时题库、真实本地 SQLite 和确定性模型替身验证，没有用 live 题库写入或服务部署作为验收手段。测试中的预期故障日志不代表测试失败。

Agent 启动器沿用现有采集开关，Recorder 的 `main` 根默认与 `search.ROOT` 一致。诊断 CLI 新命令及版本语义已同步契约和历史 runbook；根目录 `SKILL.md` 描述的搜索、路径修复与答案复制行为不变，旧飞书入口不需要迁移。题库引用诊断明确不走检索路径修复。

以上是 4.5.2 提交时的验收记录，当时 4.5.3～4.5.5 尚未开始。4.5.2 本身没有异步队列或请求等待预算，不能据此宣称已消除同步采集延迟。修改仅在独立 worktree 本地提交，未推送、合并、部署或开启生产采集。

## 4.5.3 异步采集

`AsyncCheckpointRecorder` 是启用采集时启动器统一注入 A2/A3 的入口。应用 lifespan 启动一个固定消费者；请求侧只验证、冻结必要输入并尝试入队。前驱查询及其审计、图片摘要/解码、A3 Prompt 读取、Artifact/Checkpoint 写入和 Trace 关联均由消费者执行。同步 Recorder 保留为底层执行器和旧契约测试入口，生产启动器不提供同步回退选项。

| 限制 | 默认值与含义 |
| --- | --- |
| 数量 | 128，包含正在处理的 1 项，阻塞期间仍计入容量 |
| 字节 | 8 MiB，按冻结输入、完整 context 编码和每项固定记账余量累计；不是进程 RSS 承诺 |
| 年龄 | 120 秒，单调时钟，在出队及前驱查询后检查；过期任务不开始后续采集 |
| 请求提交预算 | 100ms，累计 A3 父阶段、并发 unit 和子 A2 的冻结/入队准备耗时；不把阶段间模型等待计入，不在每阶段重置 |
| 消费者 | 每个 Recorder 固定 1 个；没有每请求新线程、同步回退或采集重试 |

这些是显式代码默认值，可在隔离代码构造器中调整，现有生产 CLI 的采集开关与七项 Store 容量参数保持原样。后续容量校准归 4.5.5；100ms 是尽力而为的提交预算，不能取消已经执行的 CPU 或 I/O，也不等于测得的 P99。

`CAPTURE_QUEUED` 只表示已接纳，返回 `stored=False` 且没有 `checkpoint_id`。只有 Store 成功提交后的对象才能发布 Trace 关联，事件明确使用该对象的 trace/request/owner，不继承消费者线程的可变上下文。队列满、字节满、超龄、请求预算耗尽、清理竞争和关闭分别返回稳定原因；健康信息包含 queued/stored/rejected/expired/failed、pending、pending_bytes、worker_alive，公共健康投影同步 pending 和 queue_capacity。

消费者用经过 Store 权限、TTL、完整性和审计的查询，分别取得最近已提交和最近成功节点。严格按 identity/session/workflow/search/unit 及父子 revision 匹配，允许原有逻辑任务内 candidate generation 随阶段更新；每个任务保留自己的完整冻结 owner。没有本 scope 前驱时只按合法父 unit、父页回溯，失败或丢弃的写入不能生成虚构前驱。队列按接纳顺序处理并发到达的阶段，不为缺失阶段补造结果。

原图和裁图使用最长 120 秒的进程内资源租约，数量受同一队列限制。会话清理只保留已有有效租约的不可变文件，完成或丢弃后释放并删除已被清理标记的旧文件；后续创建的新文件名不受影响。清理进行时新租约直接拒绝，清理过程不持有队列锁。过期租约不阻止正常清理；文件缺失明确降级，不查找另一版本。此机制不提供进程崩溃恢复。

本次只接入必要的启动/关闭顺序：先停止接纳并尝试等待消费者 2 秒，再交还应用生命周期。正在执行的 Python I/O 不能强杀；Trace 关闭、维护等待预算、熔断、完整有限停机与运行任务超时协调仍由 4.5.4 验收，本节不宣称 F1/F2 已完成。

验收以 `tests/test_checkpoint_async.py` 的 Q1/Q2/Q3 断言、启动器测试和完整回归为准。日志保存在仓库外 `F:\cc\_backups\7-题库检索\2026-09-08\checkpoint-4.5.3-validation\`。未推送、合并、部署或启用现有服务采集。

### 4.5.3 验收结果（2026-09-08）

| 验收 | 当前证据 |
| --- | --- |
| Q1 | 用 Event 暂停图片读取、打开的写事务，并分别持有真实 Store 锁和共享 Trace 路径锁；释放阻塞前业务已返回，恢复后五阶段成功入库 |
| Q2 | 同时计算在途和排队数量/字节；满队列、满字节、超龄、预算耗尽及关闭明确拒绝或丢弃；连续提交保持同一个消费者；完成后 pending 和 pending_bytes 归零 |
| Q3 | 消费者阻塞期间完成搜索、立即取答案、再开始新搜索；提交后 Trace 和 revision 对应原请求；多 unit 乱序完成并立即选题取答案仍保持各自 owner/前驱；失败写入没有 Trace ID 或虚构前驱，最近提交与最近成功分开维护 |
| 资源与接线 | A3/子 A2 共用请求预算和消费者；清理保留租约资源并拒绝新租约，释放后保留新建文件；真实 Windows junction 不被跟随；lifespan 启动/关闭和默认禁用保持兼容 |

完整回归 `python -B -m unittest discover -s tests -q`：1444 项通过，77.895 秒。最后补强清理链接检查及 2 项资源测试后，运行以下专项命令，21 项通过（其中异步验收 16 项、启动器 5 项）：

```powershell
python -B -m unittest -q tests.test_checkpoint_async tests.test_a2_checkpoint_launcher tests.test_a3_checkpoint_launcher
git diff --check
```

全仓日志中的故障注入 traceback、CLI 参数拒绝和模型替身警告属于预期测试场景，最终结果为 OK。验证未访问外部模型或改动 live 题库。4.5.4 和 4.5.5 尚未开始，不把这次 Q1～Q3 通过扩大为后台故障治理、完整停机或实际压力场景已验收。
