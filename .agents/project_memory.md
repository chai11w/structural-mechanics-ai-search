# Project Memory

## Current State

- 项目是结构力学题库检索器；CLI、飞书和 Agent 共用检索核心但运行状态隔离。8788 是飞书搜题，8790 是邀请制网页主线，8793 是评审镜像，8794 是隔离 Agent，8795 是管理员后台。
- 8790 进程、看门狗和登录任务均读取 8795 共享控制库；后台新码登录和动态停用已验证，原静态邀请码配置仅作灾备。8795 由独立登录任务和看门狗保活。
- 8795 已实现管理员登录、邀请码与动态额度、用量/费用概览、反馈和对话详情；邀请码与反馈支持归档、恢复及受保护删除。
- 反馈案例保存用户可见对话和题目/候选/答案图片，默认 30 天清理；不保存邀请码明文、隐藏 Prompt、内部推理、密钥或本地路径。
- 8790 保持 1 个活动任务、2 个排队任务、最长等待 55 秒；费用按 Asia/Shanghai 自然日，金额是项目估算，不等同供应商账单实扣。
- 当前题库共 280 行，只有 24 行满足 >20，均属“4力法—钢架—均布 q”组；真实覆盖仍窄。
- 统一五状态请求协议 477 项测试通过，已接入正式 8790；正式认证、`SUCCESS`、`NEEDS_INPUT` 和反馈绑定闭环均已验证。8795 请求报表和协议时间线已按用户决定回溯，后台保持原功能，后续未经单独确认不再扩展。

## Implemented

- 尺寸模块负责解析、约束、缓存和 Qwen 识别；核心入口共享荷载检索、尺寸复筛和视觉重排。尺寸不区分横竖，缺边不按比例补齐，拱不复筛。
- 字母库全局搜索仅在章节判断失败且用户授权后执行；支持章节问题走零模型代码事实回答，混有搜题指令时仍走 Intent V2。
- 8790 具备有界执行、同会话串行、过期会话清理、媒体 no-store、安全响应头及忙碌/额度中文错误。
- 手机网页上传前将大图压到约 1MB；共享图片编码器覆盖 8788、8790、8794 和图片 CLI，大图限制最长边 2560px、必要时 2048px，源文件不变。8793 保留稳定快照。
- 费用台账按稳定邀请码身份记录次数和估算费用，并兼容旧 SQLite schema。
- 反馈支持稳定编号、耗时、可见对话/媒体、章节、归档、处理状态和到期清理；编号可供 Codex 只读定位回复、费用与状态。
- 管理员密码和 8790 邀请码仅存哈希；8795 用 AES-GCM 保存新建/重置邀请码，明文复制受管理员会话、CSRF 和审计保护。
- 8795 提供概览、邀请码、反馈及设置；后台登录任务由独立看门狗保活。旧邀请码导入和 8790 控制库切换均默认预检，显式执行前备份并支持失败回退。
- 8790、8794 已接入并行外荷载 A/B 筛查：A 用智谱只判 `yes/no`，15 秒内的 `no` 可在候选提交前中断 B；候选先提交则直接返回，章节追问/空结果等待 A，A 超时或失败保留 B。提交栅栏阻止晚到结果覆盖状态；启动器保留关闭筛查的回退开关。
- 8790/8794 失败完成版：HTTP、流式和业务 `ERROR` 可评价并提供恢复动作；原请求可重试，`PARTIAL` 显示降级。临时会话过期直接回初始页且不评价。已接入 8790，8793 未刷新。
- `tiku_shared/request_protocol.py` 固定五状态并统一 `layer/code/retryable/action/request_id/search_id`；Agent、API、网络边界和任务日志已接入，同题重试沿用 `search_id`，每次请求使用独立 `request_id`。
- 任务日志 v2 和反馈 v4 保存统一协议字段；反馈绑定被评价回复并支持按状态、环节、原因码和请求/搜题编号筛选，旧数据只读兼容。

## In Progress

- 当前 3 条活动反馈、0 条归档、2 条待处理；可见对话链路已有真实数据，旧反馈无可靠耗时则显示“暂无数据”。
- `admin-tiku.cysbuting.cn` 已公开路由到 8795；Cloudflare Zero Trust 免费方案仍要求绑定支付方式，用户决定暂不启用 Access，当前继续使用应用层管理员密码。
- 准备在隔离 8890 将固定流水线升级为受约束自主搜题 Agent：结构化理解生成 `ProblemSnapshot`，Planner 只能选择白名单动作并按工具观察结果再规划；确定性检索、章节边界、裁剪执行和排序规则继续由代码控制。
- 真实复杂多图案例中，当前识别把辅助单位力 `Fp=1` 当作实际荷载，导致 `mixed_symbolic_numeric` 并泄露英文内部错误；新理解层必须区分原结构图、内力图和辅助单位力图，实际检索荷载只保留原结构上的 `q`。
- `8890` 已接入千问 `qwen3.7-plus` 预检影子运行；影子记录建议路线、摘要、自由观察、分流原因、耗时、令牌用量和错误，但不改变现有固定检索结果。

## Not Implemented

- `ProblemSnapshot` 契约、结构化图片理解、主结构图定位/裁剪、白名单 Planner 和工具观察循环尚未实现。
- `8890` 影子期尚未形成真实样本占比、人工对照和费用报表；影子结果不能作为正式路由门。
- 8795 暂不接入统一协议请求记录、协议筛选和反馈请求时间线；用户决定先开发隔离自主 Agent，后台扩展需另行确认。
- 8795 登录失败限速尚未实现；Cloudflare Access/WAF 因支付方式限制暂不启用。
- 桁架高度几何计算未实现；模型只抄录明确标注。
- 视觉重排同分候选缺少稳定的原粗排名次作为次级排序键。
- 真实触发覆盖尚不能证明其他章节、梁、桁架或单边尺寸效果相同。
- 云服务器和云模型扩容尚未进行；当前电脑仍是 8790 服务器。

## Architecture Rules

- 8795 独立运行在 8790 之外，管理员认证、Cookie、运行目录和控制数据不得与用户邀请码会话混用。
- 费用按稳定邀请码身份归属，不按可刷新或清除的临时会话 Cookie 归属。
- 保留单邀请码额度与全站保险上限；调整邀请码数量或单码额度时同步核算理论总暴露。
- 8790 认证始终只读取邀请码哈希；8795 可单独加密保存新建/重置邀请码的可恢复副本，列表只显示脱敏值，完整值仅经管理员会话、CSRF 和审计保护的复制接口返回。
- 旧邀请码只有哈希，无法还原；必须重置后才可复制。`control.sqlite3` 与邀请码加密密钥必须一起备份、保护和迁移。
- 迁移 8790 前先只读预检并显式合并原哈希邀请码配置；冲突时禁止写入，`--control-db` 与 `--invite-config`、静态费用上限不得混用。
- 灾备迁移或重切前必须核对旧配置与控制库的 ID、哈希、状态和认证版本；普通幂等导入不能当作线上状态或 Cookie 兼容证明。
- 预算在任务准入前检查、完成后落账；当前全局单任务并发用于防止并发穿透额度。
- 费用按目录价格和 usage 估算，缺数据时不能视为供应商实扣。
- 反馈只保留最小必要可见上下文；案例到期后清理对话和媒体，评分、原因、反馈编号、归档与处理状态继续保留。搜题耗时定义为上传题图至首批候选在页面可见的用户等待时间，不以模型费用任务起止时间代替。
- 修改检索、储存或索引逻辑时同步检查 CLI、飞书、Agent 和项目 Skill 文档。
- 题库写操作继续 plan → confirm → backup → execute；8790、8793、8794、8795 与 8788 的状态和端口保持隔离。

## Known Risks

- 新理解层尚未证明可可靠替代并行外荷载 A 层；A 应先保留并影子对照非题目、无外荷载、普通题和复杂多图题，达标后再退役，不能直接删除。
- 影子预检调用在原固定线路之后台运行，模型失败、解析失败和队列满都必须只记录，不能影响用户返回。
- A/B 取消不能撤回已发出的模型请求；提交栅栏只能阻止晚到 B 覆盖状态，仍需观察误拦截、15 秒长尾和额外费用。
- 邀请码被转发会共享同一 3 元额度；签名 Cookie 防伪造但不能阻止持码人主动共享。
- 费用完成后入账，因此最后一个在途任务可能使单码或全站金额略高于阈值。
- usage 缺失、价格目录缺项或供应商规则变化会造成估算偏差；30 元不是支付平台硬限额。
- `admin-tiku.cysbuting.cn` 已接入 Tunnel，但 Access/WAF 未启用，公网目前只有应用层管理员门禁且缺少登录失败限速。
- Qwen 冷调用有长尾；候选下降不保证首次总耗时一定下降。
- 2026-07-29 曾有旧 API 密钥出现在进程命令行和工具输出；旧进程已终止，用户当时选择暂不轮换。

## Do Not Do

- 不读取、提交或在工具输出、日志、项目文档中展示 API key、Tunnel token、邀请码明文、管理员密码、私有发放清单和本地敏感配置；管理员页面的受控复制功能除外。
- 不把管理员认证并入用户邀请码会话，不把 8795 部署进 8790 进程。
- 不因后台记录的 ID/哈希一致就假定旧邀请码可用；灾备切换还必须核对状态并验证登录和动态撤销。
- 不把邀请码身份改回会话 Cookie，也不删除全站保险上限只保留单码额度。
- 不跨章节搜索，不绕过项目脚本的识别、过滤和排序。
- 不在未授权时把图片发给外部模型，不重复已完成的题库原图付费评测。
- 不随意停止或改造 8788；部署重启必须精确限定目标端口与进程。

## Next Best Step

1. 保持 `8890` 影子运行，收集新的真实上传并对照人工标签、现有外荷载筛查和固定线路结果。
2. 汇总 A1/A2/A3 占比、纠正类型、解析失败、延迟、令牌用量和费用，形成 Stage 2 验收报表；不重复付费运行已经评测过的题库原图。
3. Stage 2 达标后补齐正式数据契约，再评估 A3 主结构图裁剪和白名单 Planner；暂不开发 LangGraph 或切换 8790。

## Important Commands

- `python -m unittest discover -s tests`
- `python scripts/run_tiku_admin.py --help`
- `python scripts/manage_tiku_admin.py --control-db .tmp_tiku_admin_8795/control.sqlite3 --import-invites <原哈希配置> --require-status-match`（灾备切换前严格只读预检）
- 普通导入确认后添加 `--apply-import`（显式补入缺失记录，不覆盖后台状态）
- `powershell -ExecutionPolicy Bypass -File scripts/switch_tiku_agent_8790_control.ps1`（灾备重切前只读预检）
- `powershell -ExecutionPolicy Bypass -File scripts/tiku_admin_watchdog_8795.ps1`
- `python scripts/run_tiku_agent_demo.py --help`
- `python scripts/manage_tiku_invites.py --help`
- `python scripts/search_by_loads.py --help`
- `python search.py --help`
