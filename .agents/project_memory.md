# Project Memory

## Current State

- CLI、飞书和 Agent 共用检索核心、状态隔离。8788 是飞书，8790 是邀请网页主线，8793 是评审镜像，8794 是隔离 Agent，8795 是管理后台；8891 保留权威分流 MVP，8896 接通分流和 A3 人工裁剪。
- 8790 读取 8795 共享控制库，原静态邀请码配置仅作灾备；8795 由独立登录任务和看门狗保活，管理邀请码、额度、费用和反馈，案例只保留用户可见对话与媒体、默认 30 天清理，不保存密钥、隐藏提示词或内部推理。
- 8790 保持 1 个活动任务、2 个排队任务、最长等待 55 秒；费用按 Asia/Shanghai 自然日，金额是项目估算，不等同供应商账单实扣。
- 字母库共 280 行（2-7 章），只有 24 行满足 >20，均属“4力法—钢架—均布 q”组；live 主库另有 497 行（2-8 章），真实覆盖仍窄。

## Implemented

- 共享层：尺寸模块负责解析、约束、缓存和 Qwen 识别（尺寸不分横竖、缺边不按比例补齐、拱不复筛），核心入口共享荷载检索、尺寸复筛和视觉重排；图片编码器覆盖 8788/8790/8794/图片 CLI，手机大图上传前压到约 1MB（最长边 2560px、必要时 2048px），源文件不变；8793 保留稳定快照。
- 字母库全局搜索仅在章节判断失败且用户授权后执行；支持章节问题走零模型代码事实回答，混有搜题指令时仍走 Intent V2。
- 8790 具备有界执行、同会话串行、过期清理、媒体 no-store、安全响应头和中文错误提示；HTTP/流式/业务错误可恢复、评价或重试，部分结果显示降级；8793 未刷新。
- 费用按稳定邀请码记录次数和估算金额；反馈支持稳定编号、耗时、可见内容、章节、归档、处理状态和到期清理。
- 管理员密码和 8790 邀请码仅存哈希；8795 用 AES-GCM 保存新建/重置邀请码，明文复制受管理员会话、CSRF 和审计保护。
- 旧邀请码导入和 8790 控制库切换默认只读预检，执行前备份并支持失败回退。
- 8790/8794 已接入并行外荷载 A/B 筛查：A 只判 `yes/no`，15 秒内的 `no` 可在候选提交前中断 B；候选先提交则直接返回，A 超时或失败保留 B；提交栅栏阻止晚到结果覆盖状态，启动器保留关闭开关。
- `tiku_shared/request_protocol.py` 固定五状态并统一 `layer/code/retryable/action/request_id/search_id`；Agent、API、网络边界、任务日志和反馈绑定已接入，同题重试沿用 `search_id`，旧数据只读兼容。
- `8891` 已完成隔离权威分流 MVP：A2 进入原检索，A1/A3 只生成受约束说明；分离绘制的荷载按结构语义归属。
- `8892` 是无端口、始终 `review_required` 的 Paddle 离线质检器；已证明检测框和多尺度窗口能召回部分子图，但竖排拆分与完整性仍不可靠，现降为 V2。
- `8896` 已接通 A1/A2/A3：复用 8891 权威预检，A1 停止，A2 以 `prechecked_single` 进入原检索，A3 才人工裁剪。整页层只取题号/题干和图形绑定，裁剪通过后由 A2 识别荷载并传递父题、公共题干和子题标题。候选/答案显示原题标签；无题号单元按整页顺序生成唯一显示名；“选择”确认候选，“换题重新搜”切换原图题目。
- A3 未通过只在裁剪区显示“结果 + 主因”，通过后先显示用户裁剪图再显示 A2 回复；选题等区内动作不重复写入对话，并保留“继续裁剪”。手机裁剪框支持新建、整体移动和四角缩放，四角使用 44px 触控区，题号居中显示在裁剪标题正下方。上传新题或切换子题只清当前状态，既有题图、裁图、候选和答案保留到新建对话或 2 小时过期；失效媒体只原地提示，旧错误/A3 回显自动清理，全局错误跨刷新去重。
- A3 真实回归覆盖 4 张页面，候选结果为 `1 searchable + 1 uncertain`、4、4、9；第 20 题已跑通裁剪、章节 2、候选、答案和剩余 3 题返回。组级标题及完整分流回归已覆盖，全量 609 项测试通过。

## In Progress

- 8896 完整分流已接通，进入真实样本验收；先确认明显单题走 A2 且不出现裁剪，再继续收集 A3 裁剪 `review_required`、章节追问、候选/原题切换和多题连续检索失败样本。
- Paddle 候选、语义对齐、自动裁剪和人工回退仍属于 A3 V2；人工裁剪 MVP 稳定前继续暂停 splitter 优化。

## Not Implemented

- Paddle 局部切片 splitter、自动裁剪和自动/人工回退属于 V2，尚未实现；既有 `8892` 试验结论保留但当前不继续优化。
- `8890` 影子期已有首份小样本统计，但未完成费用报表；影子结果不能直接提升到 8790 正式线路。
- 8795 登录失败限速未实现；Cloudflare Access/WAF 因支付方式限制暂不启用。
- 桁架高度几何计算未实现；模型只抄录明确标注。
- 视觉重排同分候选缺少稳定的原粗排名次作为次级排序键。
- 真实触发覆盖尚不能证明其他章节、梁、桁架或单边尺寸效果相同。
- `8891` 真实验证只覆盖 A1、A3 一题多图和一张 A2 单题；分离绘制荷载误判提示词已修正但待用户重传复验，大样本准确率未知。
- 云服务器和云模型扩容尚未进行；当前电脑仍是 8790 服务器。

## Architecture Rules

- 8795 独立运行在 8790 之外，管理员认证、Cookie、运行目录和控制数据不得与用户邀请码会话混用。
- 费用按稳定邀请码身份归属，不按可刷新或清除的临时会话 Cookie 归属。
- 保留单邀请码额度与全站保险上限；调整邀请码数量或单码额度时同步核算理论总暴露。
- 8790 认证始终只读取邀请码哈希；8795 可单独加密保存新建/重置邀请码的可恢复副本，列表只显示脱敏值，完整值仅经管理员会话、CSRF 和审计保护的复制接口返回。
- 旧邀请码只有哈希，必须重置后才可复制；`control.sqlite3` 与加密密钥必须一起保护和迁移。
- 迁移 8790 前只读核对旧配置与控制库的 ID、哈希、状态和认证版本；冲突禁止写入，不能把幂等导入当作线上兼容证明。
- 预算在任务准入前检查、完成后落账；当前全局单任务并发用于防止并发穿透额度。
- 费用按目录价格和 usage 估算，缺数据时不能视为供应商实扣。
- 反馈只保留最小必要可见上下文；案例到期后清理对话和媒体，评分、原因、编号、归档与处理状态保留。搜题耗时定义为上传题图至首批候选页面可见的等待时间。
- 修改检索、储存或索引逻辑时同步检查 CLI、飞书、Agent 和项目 Skill 文档。
- live 题库根为 `D:\桌面\答疑、帮做\结构力学\帮做`，字母库为旁边的 `帮做_字母库`；仓库根同名章节 Excel 是历史跟踪副本、行数与 live 不一致，写操作必须针对 live 根。`config.local.json` 当前无 `root`，`search.py` 回退该默认根。
- 题库写操作继续 plan → confirm → backup → execute；8790、8793、8794、8795 与 8788 的状态和端口保持隔离。

## Known Risks

- 新理解层尚未证明可替代并行外荷载 A 层；A 应先保留并影子对照非题目、无外荷载、普通题和复杂多图题，达标后再退役。
- A3 已使用 `unit_id` 和双向图引用，但真实回归仍只有 4 张整页、1 道完整裁剪链路；密集页面模型调用较慢，裁剪边界过紧时校验器可能要求用户扩大范围。
- 影子预检在固定线路后台运行；模型失败、解析失败和队列满都只记录，不影响用户返回。
- A/B 取消不能撤回已发出的模型请求，仍需观察误拦截、15 秒长尾和额外费用。
- 邀请码被转发会共享同一 3 元额度；签名 Cookie 防伪造但不能阻止持码人主动共享。
- 费用完成后入账，因此最后一个在途任务可能使单码或全站金额略高于阈值。
- usage 缺失、价格目录缺项或供应商规则变化会造成估算偏差；30 元不是支付平台硬限额。
- `admin-tiku.cysbuting.cn` 已接入 Tunnel，但 Access/WAF 未启用，公网目前只有应用层管理员门禁且缺少登录失败限速。
- Qwen 冷调用有长尾；候选下降不保证首次总耗时一定下降。
- 2026-07-29 曾有旧 API 密钥出现在进程命令行；旧进程已终止，用户暂不轮换。

## Do Not Do

- 不读取、提交或在工具输出、日志、项目文档中展示 API key、Tunnel token、邀请码明文、管理员密码、私有发放清单和本地敏感配置；管理员页面的受控复制功能除外。
- 不把管理员认证并入用户邀请码会话，不把 8795 部署进 8790 进程。
- 不因后台记录的 ID/哈希一致就假定旧邀请码可用；灾备切换还必须核对状态并验证登录和动态撤销。
- 不把邀请码身份改回会话 Cookie，也不删除全站保险上限只保留单码额度。
- 不跨章节搜索，不绕过项目脚本的识别、过滤和排序。
- 不在未授权时把图片发给外部模型，不重复已完成的题库原图付费评测。
- 不随意停止或改造 8788；部署重启必须精确限定目标端口与进程。

## Next Best Step

1. 在 `http://127.0.0.1:8896` 重新上传明显单题样本，确认权威预检走 A2、跳过裁剪并进入原候选流程。
2. 用真实多题页复测 A3 的“选择这个候选”“换题重新搜”和裸题号澄清，再继续收集裁剪主因映射样本。
3. 人工裁剪闭环稳定后再启动 Paddle 自动裁剪 V2。

## Important Commands

- `python -m unittest discover -s tests`
- `powershell -ExecutionPolicy Bypass -File scripts/tiku_admin_watchdog_8795.ps1`
- `python scripts/run_tiku_agent_demo.py --help`
- `python -B scripts/run_tiku_agent_8896.py`
- `python scripts/inspect_paddle_layout_candidates.py --help`
- `python scripts/search_by_loads.py --help`
- `python search.py --help`
