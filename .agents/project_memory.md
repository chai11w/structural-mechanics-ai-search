# Project Memory

## Current State

- A3-V1 已提升到 `8896`：Qwen 整页理解 → GLM 一次框选全部题 → 用户多选/单题自动下行 → 所选裁图并发 Qwen 校验/外荷载门禁 → A2 或原人工裁剪回退；`8897` 暂留同能力的独立回退线。
- 端口隔离：8788 飞书、8790 网页、8794 隔离 Agent、8795 管理后台；8891 权威分流，8896 A3-V1 主线，8897 回退/对照。
- 8790 读取 8795 控制库；单任务 1 活动/2 排队、55 秒等待，费用按 Asia/Shanghai 自然日估算。
- live 题库目前可检索内部目录 `2静定结构`～`8影响线` 七类；字母库覆盖仍窄。

## Implemented

- A3-V1 离线对照使用 `D:\桌面\A3` 8 图比较 1/2/3A/3B/GLM；GLM 8/8 成功、输出 27 个裁图，人工复核显著更优。评测脚本 `evaluate_a3_crop_strategies.py` 支持 Qwen/MiMo/GLM，但旧结果不等同生产能力。
- 8896/8897 用严格 `a3-page-crops-v1` 绑定既有 `unit_id` 并允许部分成功；保存干净裁图和标签叠加图。只对用户多选目标并发（上限 10）执行 Qwen 完整性校验与独立外荷载门禁；失败/无框/异常回退人工并可预填 GLM 框。
- 8897 实测发现 GLM 回显标签差异会被旧契约整页拒绝；现只以第一步生成、第二步原样返回的完整唯一 `unit_id` 集合作为权威绑定，`question_label` 无条件统一为服务端标签，框图语义错误交给后续 Qwen 校验。整页零 `auto_ready` 时直接回到 V0 选题与人工裁剪，不再展示无意义的自动裁图预校验；只要 GLM 返回了 bbox，仍保存裁图、人工裁剪预填框和整页框选图。
- A3 题目进入 A2 检索后，用户选择“换题重新搜”会将原题记为已检索并关闭；`searched_unit_ids` 与真正已拿到答案的 `completed_unit_ids` 分开保存，前后端同时禁止重复进入。
- A3-V1 只有 1 个可检索题目且 GLM 有自动裁图时，不再要求用户进入选题多选页；后端直接执行 Qwen 完整性校验和外荷载门禁，通过后进入 A2，失败才回退人工裁剪。
- 8897 独立使用端口、Cookie、SQLite、session/media、日志和 watchdog；`evaluate_a3_v1_flow.py` 可模拟全选并生成复核页。全量 `700/700` 测试及桌面/390px 浏览器渲染通过。
- 共享层负责尺寸解析/约束/缓存及大图压缩；视觉复筛无满分时取粗筛前 3，满分不足 3 时补齐，失败回退粗筛。共享展示为综合分 `>=90%` 全部，否则可靠 Top 3；8896 独立按 Qwen 原始复筛分排序，`>=95%` 全部，否则 Top 3，粗筛仅作同分辅助，并发 10。8793 保留稳定快照。
- 视觉复筛支持 Zhipu `glm-4.6v` 或 DashScope Qwen `qwen3.7-plus`；隔离 8896 A2 使用 Qwen + V1，A3 独立外荷载门禁也已从 Zhipu 切至相同 Qwen 模型。8788/8790/CLI 共享默认仍为 Zhipu，V4/V5 暂不切换。
- 共享章节目录保留七个存储键和 `supported|unsupported|uncertain` 三态；方法名优先，`结点法`、`节点法`、`截面法` 映射到 `2静定结构`，并排除“超/不/非静定”误匹配。图片范围依次检查非中文、题干范围和合法高置信提示，纯公式按无题干处理；60 条章节评测通过。
- 字母库全局搜索仅在章节判断失败且用户授权后执行；支持章节问题走零模型代码事实回答，混有搜题指令时仍走 Intent V2。
- 8790 具备有界执行、串行、过期清理、媒体 no-store、安全响应头和失败降级；8793 未刷新。
- 费用按稳定邀请码估算；反馈到期清理。
- 管理员密码/邀请码只存哈希；8795 新码用 AES-GCM，受会话、CSRF、审计保护。
- 8790/8794 已接入并行外荷载 A/B 筛查：A 的 `no` 可在 15 秒内、候选提交前中断 B；否则保留 B，提交栅栏阻止晚结果覆盖，启动器可关闭。
- 请求协议固定五状态；Agent、API、网络边界、任务日志和反馈已接入，同题重试沿用 `search_id`，旧数据只读兼容。
- `8891` 已完成隔离权威分流 MVP：A2 进入原检索，A1/A3 只生成受约束说明。
- `8892` 是始终 `review_required` 的 Paddle 离线质检器；可召回部分子图，但竖排拆分和完整性不可靠，现降为 V2。
- `8896` 已接通并默认启用 A3-V1；保留 `--disable-auto-crop` 原位回退到 V0 人工裁剪。A1 停止，A2 原检索，A3 共用 Qwen 外荷载门禁和严格章节三态，异常 fail-closed；其他入口未启用该开关。
- A3 未被图引用的空分组会被移除，其文字进入 `unassigned_content` 并记录含 `group_id` 的 warning；被图引用的空组和移除后全空页面仍拒绝。修复后密集页 `5-9`～`5-17` 实测：图片分类与整页理解各 1 次 Qwen 调用即成功，无 schema 重试；尾部 `5-18`～`5-19` 空组由本地规则处理。
- A3 schema 错误只存 code/简因 30 天，不存模型原文或用户内容；错误态等待 100 秒，普通文字 60 秒。题图、裁图、候选和答案在新对话或 2 小时后过期。

## In Progress

- 8897 已由用户完成真实页面手测并确认全流程成功；8896 提升后的真实页面验收由用户继续执行，助手不主动外发图片或产生模型费用。
- Paddle 候选、语义对齐、自动裁剪和人工回退属于 A3 V2；MVP 稳定前暂停 splitter 优化。
- 视觉复筛对照已完成：V1+Qwen 保持生产；V4/V5 固定集召回不足，不切换；费用以供应商账单为准。

## Not Implemented

- Paddle 局部切片 splitter、自动裁剪和自动/人工回退属于 V2，尚未实现；既有 `8892` 试验结论保留但当前不继续优化。
- `8890` 影子期已有首份小样本统计，但未完成费用报表；影子结果不能直接提升到 8790 正式线路。
- 桁架高度几何计算未实现；模型只抄录明确标注。
- 视觉重排评分校准未完成；V5 的结构/荷载联合规则损失召回，不作生产 Prompt。后续应增加真实题图，并针对荷载位置和支座差异单独构造样本。
- “首次复筛结果超过 5 个时，用独立荷载位置 Prompt 二次复筛”仅是候选方案；先收集真实多候选样本。
- 真实触发覆盖尚不能证明其他章节、梁、桁架或单边尺寸效果相同。

## Architecture Rules

- 8795 独立于 8790；管理员认证、Cookie、运行目录和控制数据不得与用户会话混用。
- 费用归属稳定邀请码，不按临时 Cookie；保留单码额度和全站上限，调整时核算理论总暴露。
- 8790 只读邀请码哈希；8795 可加密保存新建/重置码，完整值仅由受会话、CSRF 和审计保护的接口返回。旧码须重置后才可复制，控制库与密钥必须一起迁移。
- 迁移 8790 前只读核对旧配置与控制库的 ID、哈希、状态和认证版本；冲突禁止写入。
- 预算在任务准入前检查、完成后落账；当前全局单任务并发用于防止并发穿透额度。
- 费用按目录价格和 usage 估算，不等同供应商实扣。
- 反馈只留必要上下文并到期清理；搜题耗时从上传至首批候选可见。
- 修改检索、储存或索引逻辑时同步检查 CLI、飞书、Agent 和项目 Skill 文档。
- live 题库根为 `D:\桌面\答疑、帮做\结构力学\帮做`，字母库为相邻 `帮做_字母库`；仓库 Excel 是历史副本，写操作必须针对 live 根。
- 题库写操作继续 plan → confirm → backup → execute；8790、8793、8794、8795 与 8788 的状态和端口保持隔离。

## Known Risks

- GLM 裁图的固定样本和用户实测仍有限；需继续验证多题混排、题图分离、小支座/小荷载、低清和旋转图片。8896 的 `--disable-auto-crop` 与 8897 独立线在观察期内不得移除。
- 旧 `parse_chapter` 仍会把“第4章”映射为内部 `4力法`；8896 已改用严格目录并对纯数字返回 `uncertain`，其他未迁移入口直接复用旧接口仍可能误搜。
- 新理解层尚未证明可替代并行外荷载 A 层；A 应先保留并影子对照非题目、无外荷载、普通题和复杂多图题，达标后再退役。
- A3 已使用 `unit_id` 和双向图引用并安全处理未引用空组，但真实样本仍少；密集页较慢，裁剪过紧时仍可能要求扩大范围。
- 影子预检在固定线路后台运行；模型失败、解析失败和队列满都只记录，不影响用户返回。
- A/B 取消不能撤回已发出的模型请求，仍需观察误拦截、15 秒长尾和额外费用。
- 邀请码被转发会共享同一 3 元额度；签名 Cookie 防伪造但不能阻止持码人主动共享。
- 费用完成后入账，最后一个在途任务可能略超阈值；usage/价格缺失也会造成估算偏差，30 元不是支付平台硬限额。
- `admin-tiku.cysbuting.cn` 已接入 Tunnel，但 Access/WAF 未启用，公网目前只有应用层管理员门禁且缺少登录失败限速。
- Qwen 冷调用有长尾；V1+Qwen 首轮平均约 1.93 秒，仍需观察 8896 长尾和限流；8788/8790 暂不切换。
- 2026-07-29 曾有旧 API 密钥出现在进程命令行；旧进程已终止，用户暂不轮换。

## Do Not Do

- 不读取、提交或在工具输出、日志、项目文档中展示 API key、Tunnel token、邀请码明文、管理员密码、私有发放清单和本地敏感配置；管理员页面的受控复制功能除外。
- 不把管理员认证并入用户邀请码会话，不把 8795 部署进 8790 进程。
- 不因后台记录的 ID/哈希一致就假定旧邀请码可用；灾备切换还必须核对状态并验证登录和动态撤销。
- 不把邀请码身份改回会话 Cookie，也不删除全站保险上限只保留单码额度。
- 不跨章节搜索，不绕过项目脚本的识别、过滤和排序。
- 不在未授权时把图片发给外部模型，不重复已完成的题库原图付费评测。
- 不随意停止或改造 8788；部署重启必须精确限定目标端口与进程。

## Next Best Step

1. 由用户在 8896 继续验收真实题页，重点记录多题混排、题图分离、小荷载/支座、低清和旋转边界失败。
2. 观察期内保留 8897 和 `--disable-auto-crop`；确认 8896 稳定后再停止 8897，但不删除其运行状态。
3. 继续验收章节追问、题目切换、已检索题关闭和连续检索失败。

## Important Commands

- `python -m unittest discover -s tests`
- `powershell -ExecutionPolicy Bypass -File scripts/tiku_admin_watchdog_8795.ps1`
- `python scripts/run_tiku_agent_demo.py --help`
- `python -B scripts/run_tiku_agent_8896.py`
- `python -B scripts/run_tiku_agent_8897.py`
- `python scripts/evaluate_a3_v1_flow.py --images-dir D:\桌面\A3 --output-dir output\a3_v1_flow_desktop_A3_20260820_v1`
- `python scripts/inspect_paddle_layout_candidates.py --help`
- `python scripts/search_by_loads.py --help`
- `python search.py --help`
- `python scripts/evaluate_rerank_matrix.py --dry-run --prompts v1 v4 --providers zhipu qwen --workers 10`
