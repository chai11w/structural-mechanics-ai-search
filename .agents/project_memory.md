# Project Memory

## Current State

- A3-V1 已在 `8790` 生产：Qwen 整页理解 → GLM 全题框选 → Qwen 并发完整性/外荷载门禁 → 单题下行或多题选择 → A2/人工裁剪。
- 端口隔离：8788 飞书、8790 生产、8794 Agent、8795 后台；8891 分流，8896 验收，8897 回退。
- 8790 读取 8795 控制库认证；A1/A2/A3 与子 A2 统一计费并按题汇总流程/反馈。旧有界并发队列未恢复。
- 新 Agent/Web 统一输出和媒体边界已实现、回归并在 8790/8896 运行：公共出口白名单投影，候选原子交付，答案区分 0/部分/全部，媒体失败可重发；8788 不变。
- Trace Phase 1 审计与 2.2 传播已完成：服务端权威 `trace_id` 贯通 HTTP/stream/线程/A3/A2/tool/task/cost；下一步是 2.3 结构化事件与终态一致性。
- live 题库覆盖 `2静定结构`～`8影响线`；字母库仍窄。

## Implemented

- A3-V1 用 `a3-page-crops-v1` 和唯一 `unit_id` 绑定整页题目、GLM 框及裁图；多题并发校验（上限 10），单题自动下行 A2，零可用结果或异常回到人工裁剪。服务端只信原样 `unit_id` 集合，语义由 Qwen 门禁复核。
- A3 区分已搜索与已完成题目，题号稳定且禁止重复进入；Intent V1 在 8790 使用确定性规则，8896 保留模型兜底。取消当前题、结束整页、清空会话和“继续”均按状态与编号空间校验。
- 未引用空分组会删除并记录简化 warning；被引用空组或处理后全空页面拒绝。schema 错误仅存 code/简因 30 天，不保存模型原文或用户内容；题图、裁图、候选和答案在新对话或 2 小时后过期。
- 共享复筛：综合分 `>=90%` 全部，否则可靠 Top 3；8896/V1+Qwen 原始分 `>=95%` 全部，否则 Top 3。V4/V5 召回不足，未切换生产。
- 章节使用七个存储键和 `supported|unsupported|uncertain` 三态；方法名映射到 `2静定结构`，排除“超/不/非静定”。章节失败后只有用户授权才全局搜字母库；支持代码事实回答。
- 反馈 schema v7 区分 `page|question`；正常 UI 会校验目标，但 API 可省略 conversation 绕过目标绑定，且服务端无权威 response row。案例媒体独立保存，8795 按范围展示并兼容父/子 artifacts。
- 今日使用按上传父 workflow 统计发起页数、按进入 A2 的题目统计实际检索题数，同页多题分别计题、同题重试不重复；保留 `today_searches` 兼容别名。
- 费用归属稳定邀请码，父 A3 与子 A2 共用账本；8795 兼容旧账本并只读补算缺价调用。认证只存哈希，新邀请码用 AES-GCM，复制受会话、CSRF 和审计保护。
- 请求协议固定五状态；Agent 工具非成功结果按注册 code 生成固定安全中文，未知 code 按状态回退；内部 `error/data/safe_facts` 不进入公共序列化。HTTP/流式/A3 隐藏动态异常、路径、模型 reason 和凭据特征，正常白名单进度原样通过；同题重试沿用 `search_id`，旧数据只读兼容。新 Agent/Web/A3 媒体投影验证持久化文件和公共解析，候选原子交付，答案区分 0/部分/全部。
- Trace 2.2 聚焦 `122/122`、全仓 `815/815`；生产 FastAPI 0.139.0。8790/8896 本批未重启，8788 未改动。

## In Progress

- Trace 2.2 已完成；下一批 2.3 双写最小事件流，统一 JSON/stream、A2/A3、媒体后处理终态，并记录 fail-open writer 健康计数。

## Not Implemented

- 旧有界并发队列尚未接回 8790；当前全局单任务并发仍用于防止额度穿透。
- Paddle 局部切片 splitter、自动裁剪及自动/人工回退属于 A3 V2，暂不继续优化。
- 8890 影子期未完成费用报表，影子结果不能直接提升到 8790。
- 桁架高度几何计算未实现；模型只抄录明确标注。
- 视觉重排评分校准未完成；“候选超过 5 个后二次荷载位置复筛”只是待真实样本验证的方案。
- 现有真实触发覆盖不能证明其他章节、梁、桁架或单边尺寸均有相同效果。
- 工具错误的模型润色与薄看门狗未接入；先观察结构化固定文案的真实样本，不因功能可做就默认需要实现。
- Trace 事件库/权威终态、任务快照、阶段结果、幂等和 HTTP 解耦未实现；暂停/继续仍延后。

## Architecture Rules

- 8795 独立于 8790；Trace Store/诊断查询也独立于 8795，后者只可做可选只读 UI。管理员认证、Cookie、运行目录和控制数据不得与用户会话混用。
- 费用归属稳定邀请码，不按临时 Cookie；保留单码额度和全站上限。预算准入前检查、完成后落账；目录价格和 usage 只是估算，不等同供应商实扣。
- 8790 只读邀请码哈希；8795 可加密保存新建/重置码。控制库与 AES-GCM 密钥必须一起迁移；旧哈希码须重置后才可复制。
- 迁移控制数据前只读核对 ID、哈希、状态和认证版本；冲突禁止写入。
- 反馈只保留必要上下文并到期清理；搜题耗时从上传至首批候选可见。反馈媒体必须复制到案例目录，不依赖短期会话路径。
- A3 反馈以目标回复自己的 revision、候选数和费用上下文为准，不用会话最新状态覆盖历史快照。
- 修改检索、储存或索引逻辑时同步检查 CLI、飞书、Agent、8795 和项目 Skill 文档。
- 工具内部诊断与公共输出必须分层；新 Agent 的 HTTP/Web 出口只接受注册错误码和白名单字段，个人飞书入口不纳入这一公共边界改造。
- live 题库根为 `D:\桌面\答疑、帮做\结构力学\帮做`，字母库为相邻 `帮做_字母库`；仓库 Excel 是历史副本。
- 题库写操作必须 plan → confirm → backup → execute；各服务端口、Cookie、状态、媒体和日志保持隔离。

## Known Risks

- GLM/A3 真实样本仍少，需验证多题混排、裁剪边界、小支座/小荷载、低清和旋转；观察期保留 8896 `--disable-auto-crop` 与 8897。
- 旧 `parse_chapter` 仍会把“第4章”映射为内部 `4力法`；8896 已改用严格目录并对纯数字返回 `uncertain`，其他未迁移入口直接复用旧接口仍可能误搜。
- 新理解层尚未证明可替代并行外荷载 A 层；A 应先保留并影子对照非题目、无外荷载、普通题和复杂多图题，达标后再退役。
- 影子预检失败只记录、不影响用户；A/B 取消不能撤回已发出的模型请求，仍需观察误拦截、长尾和额外费用。
- 邀请码被转发会共享同一 3 元额度；签名 Cookie 防伪造但不能阻止持码人主动共享。
- 费用完成后入账，最后一个在途任务可能略超阈值；usage/价格缺失也会造成估算偏差，30 元不是支付平台硬限额。
- `admin-tiku.cysbuting.cn` 已接入 Tunnel，但 Access/WAF 未启用，公网目前只有应用层管理员门禁且缺少登录失败限速。
- Qwen 冷调用有长尾，需继续观察 8790/8896 限流；8788 暂不切换。
- 飞书旧入口仍可能把顶层异常文字直接回复给个人用户；这是本阶段明确接受的内部入口风险，新 Agent 的公共输出保护不会覆盖它。
- 2026-07-29 曾有旧 API 密钥出现在进程命令行；旧进程已终止，用户暂不轮换。
- 8790/8896 进程早于 HEAD 最后两次行为提交启动；输出层已加载，但最新行为仍需精确重启与无费用烟测，8788 不动。
- 观测层会重复、静默丢失或语义冲突；反馈目标/费用 join 非权威，动态保留期未接入 Web，清理只在 8795 启动时执行。

## Do Not Do

- 不读取、提交或在工具输出、日志、项目文档中展示 API key、Tunnel token、邀请码明文、管理员密码、私有发放清单和本地敏感配置；管理员页面的受控复制功能除外。
- 不把管理员认证并入用户邀请码会话，不把 8795 部署进 8790 进程。
- 不因后台记录的 ID/哈希一致就假定旧邀请码可用；灾备切换还必须核对状态并验证登录和动态撤销。
- 不把邀请码身份改回会话 Cookie，不删除全站保险上限。
- 不跨章节搜索，不绕过项目脚本的识别、过滤和排序。
- 不在未授权时把图片发给外部模型，不重复已完成的题库原图付费评测。
- 不把新 Agent 的 HTTP/Web 公共输出改造扩展到个人飞书入口，也不随意停止或改造 8788；部署重启必须精确限定目标端口与进程。
- 不在目标回复缺失时保存整段反馈历史，也不把反馈专用框选图重复注入普通聊天消息。

## Next Best Step

1. 实现 2.3 最小结构化事件与终态一致性；Trace Store 和 Codex/Agent 诊断入口不得依赖 8795。
2. 精确重启 8790/8896 加载当前 HEAD，并做健康/协议无费用烟测；不改 8788。
3. 保留 8896、8897 和 `--disable-auto-crop`，继续观察真实媒体交付与复杂题页。

## Important Commands

- `python -m unittest discover -s tests -p 'test_*.py'`
- `powershell -ExecutionPolicy Bypass -File scripts/tiku_admin_watchdog_8795.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/tiku_agent_watchdog_8790.ps1`
- `python scripts/run_tiku_agent_8790.py --help`
- `python -B scripts/run_tiku_agent_8896.py`
- `python -B scripts/run_tiku_agent_8897.py`
- `python scripts/evaluate_a3_v1_flow.py --images-dir D:\桌面\A3 --output-dir output\a3_v1_flow_desktop_A3_20260820_v1`
- `python scripts/inspect_paddle_layout_candidates.py --help`
- `python scripts/search_by_loads.py --help`
- `python search.py --help`
- `python scripts/evaluate_rerank_matrix.py --dry-run --prompts v1 v4 --providers zhipu qwen --workers 10`
