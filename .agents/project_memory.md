# Project Memory

## Current State

- A3-V1 已提升到 `8790` 生产主线：Qwen 整页理解 → GLM 框选全部题 → Qwen 并发完整性校验和外荷载门禁 → 单题下行、多题选题 → A2 检索或人工裁剪回退。
- 端口隔离：8788 飞书、8790 生产、8794 Agent、8795 后台；8891 分流，8896 验收，8897 回退。
- 8790 读取 8795 控制库做邀请码认证；A1/A2/A3 与子 A2 统一计费，8795 可按同一道题汇总完整流程和反馈。旧有界并发队列尚未恢复。
- 反馈修复已在 8790/8795 上线：列表等高等距；指定旧记录由 10 条收敛为当前 revision 的 2 条；A3“已准备 N 道题”在子 A2 尚未创建时也会由父 A3 会话保存整页框选图。
- live 题库目前可检索内部目录 `2静定结构`～`8影响线` 七类；字母库覆盖仍窄。

## Implemented

- A3-V1 用严格 `a3-page-crops-v1` 绑定完整唯一 `unit_id`，允许部分成功；保存干净裁图、GLM 原框 `model_bbox` 和叠加图。实际裁图四边各扩原框尺寸 5%，单边限制为整页 1%～3%。
- 多题页默认并发（上限 10）校验全部裁图；零 `auto_ready` 回到 V0 选题/人工裁剪，单个可检索题自动校验并进入 A2。失败、无框或异常可用 GLM 框预填人工裁剪。
- GLM 回显标签不再决定整页通过；服务端只校验第一步生成、第二步原样返回的 `unit_id` 集合，统一题目标签，语义错误交给后续 Qwen 校验。
- A3 已区分 `searched_unit_ids` 与 `completed_unit_ids`；换题会关闭原题且前后端禁止重复进入。原图题号按首次识别顺序保存，不随已完成题目重排。
- A3 Intent V1 已用于 8790/8896：8790 只用确定性规则，8896 可模型兜底且仍需代码证据。取消当前题、结束整页、开始新对话相互独立；指代图片层级或题号/候选号冲突时必须澄清，“继续”可恢复裁剪、候选或整页选题阶段。
- 未引用空分组会删除并记录简化 warning；被引用空组或处理后全空页面拒绝。schema 错误仅存 code/简因 30 天，不保存模型原文或用户内容；题图、裁图、候选和答案在新对话或 2 小时后过期。
- 共享复筛：综合分 `>=90%` 全部，否则可靠 Top 3；8896/V1+Qwen 按原始分 `>=95%` 全部，否则 Top 3，粗筛仅同分辅助。V4/V5 固定集召回不足，未切换生产。
- 章节使用七个存储键和 `supported|unsupported|uncertain` 三态；方法名映射到 `2静定结构`，排除“超/不/非静定”。章节失败后只有用户授权才全局搜字母库；支持代码事实回答。
- 反馈提交会按“最近一次含图用户消息 → 目标回复”截断；目标消息缺失时 fail-closed，不再误收整段历史。旧污染记录在 8795 读取时按相同规则收敛，无需改生产数据库。
- A3“已准备 N 道题”会把稳定化的整页框选图作为反馈专用媒体保存，不重复显示在普通聊天；预选题阶段先尝试既有 A2 媒体存储，子会话不存在时回退到父 A3 artifacts，读取时兼容两处旧 URL。8795 在目标回复下展示；缺图时使用中性提示。`FB-20260822-33272F8B22` 已在在线备份后补回仍存的框选图。
- 8795 反馈表采用固定列宽和 70px 等高行；指定旧反馈隔离验收仅显示当前题图和目标回复。移动端筛选按钮固定为 40px 高并与处理状态下拉框底部对齐，不再被 toolbar 拉伸；393px 视口实测两者均为 40px 高。浏览器量测四行均为 70px，表格为 fixed layout。
- 费用归属稳定邀请码；8790 父 A3 与子 A2 共用账本，8795 兼容旧双账本并只读补算缺价调用。管理员密码/邀请码只存哈希，新码用 AES-GCM，复制受会话、CSRF 和审计保护。
- 请求协议固定五状态；Agent、API、网络边界、任务日志和反馈已接入，同题重试沿用 `search_id`，旧数据只读兼容。8891 已完成隔离权威分流 MVP；8892 Paddle 质检器固定为 review-required 的 V2 试验。
- 反馈媒体生命周期修复相关 `78/78` 项测试和全仓 `753/753` 测试通过；JS 语法检查与 `git diff --check` 通过。

## In Progress

- 继续观察 8790 的真实多题页、范围澄清、“继续”、低清/旋转及小荷载/支座边界；不主动产生模型费用。

## Not Implemented

- 旧有界并发队列尚未接回 8790；当前全局单任务并发仍用于防止额度穿透。
- Paddle 局部切片 splitter、自动裁剪及自动/人工回退属于 A3 V2，暂不继续优化。
- 8890 影子期未完成费用报表，影子结果不能直接提升到 8790。
- 桁架高度几何计算未实现；模型只抄录明确标注。
- 视觉重排评分校准未完成；“候选超过 5 个后二次荷载位置复筛”只是待真实样本验证的方案。
- 现有真实触发覆盖不能证明其他章节、梁、桁架或单边尺寸均有相同效果。

## Architecture Rules

- 8795 独立于 8790；管理员认证、Cookie、运行目录和控制数据不得与用户会话混用。
- 费用归属稳定邀请码，不按临时 Cookie；保留单码额度和全站上限。预算准入前检查、完成后落账；目录价格和 usage 只是估算，不等同供应商实扣。
- 8790 只读邀请码哈希；8795 可加密保存新建/重置码。控制库与 AES-GCM 密钥必须一起迁移；旧哈希码须重置后才可复制。
- 迁移控制数据前只读核对 ID、哈希、状态和认证版本；冲突禁止写入。
- 反馈只保留必要上下文并到期清理；搜题耗时从上传至首批候选可见。反馈媒体必须复制到案例目录，不依赖短期会话路径。
- A3 反馈以目标回复自己的 revision、候选数和费用上下文为准，不用会话最新状态覆盖历史快照。
- 修改检索、储存或索引逻辑时同步检查 CLI、飞书、Agent、8795 和项目 Skill 文档。
- live 题库根为 `D:\桌面\答疑、帮做\结构力学\帮做`，字母库为相邻 `帮做_字母库`；仓库 Excel 是历史副本。
- 题库写操作必须 plan → confirm → backup → execute；各服务端口、Cookie、状态、媒体和日志保持隔离。

## Known Risks

- GLM 裁图的固定样本和用户实测仍有限；需继续验证多题混排、题图分离、小支座/小荷载、低清和旋转图片。8896 的 `--disable-auto-crop` 与 8897 独立线在观察期内不得移除。
- 旧 `parse_chapter` 仍会把“第4章”映射为内部 `4力法`；8896 已改用严格目录并对纯数字返回 `uncertain`，其他未迁移入口直接复用旧接口仍可能误搜。
- 新理解层尚未证明可替代并行外荷载 A 层；A 应先保留并影子对照非题目、无外荷载、普通题和复杂多图题，达标后再退役。
- A3 已使用 `unit_id` 和双向图引用并安全处理未引用空组，但真实样本仍少；密集页较慢，裁剪过紧时仍可能要求扩大范围。
- 影子预检在固定线路后台运行；模型失败、解析失败和队列满都只记录，不影响用户返回。
- A/B 取消不能撤回已发出的模型请求，仍需观察误拦截、15 秒长尾和额外费用。
- 邀请码被转发会共享同一 3 元额度；签名 Cookie 防伪造但不能阻止持码人主动共享。
- 费用完成后入账，最后一个在途任务可能略超阈值；usage/价格缺失也会造成估算偏差，30 元不是支付平台硬限额。
- `admin-tiku.cysbuting.cn` 已接入 Tunnel，但 Access/WAF 未启用，公网目前只有应用层管理员门禁且缺少登录失败限速。
- Qwen 冷调用有长尾；V1+Qwen 首轮平均约 1.93 秒，需继续观察 8790/8896 长尾和限流；8788 暂不切换。
- 2026-07-29 曾有旧 API 密钥出现在进程命令行；旧进程已终止，用户暂不轮换。

## Do Not Do

- 不读取、提交或在工具输出、日志、项目文档中展示 API key、Tunnel token、邀请码明文、管理员密码、私有发放清单和本地敏感配置；管理员页面的受控复制功能除外。
- 不把管理员认证并入用户邀请码会话，不把 8795 部署进 8790 进程。
- 不因后台记录的 ID/哈希一致就假定旧邀请码可用；灾备切换还必须核对状态并验证登录和动态撤销。
- 不把邀请码身份改回会话 Cookie，也不删除全站保险上限只保留单码额度。
- 不跨章节搜索，不绕过项目脚本的识别、过滤和排序。
- 不在未授权时把图片发给外部模型，不重复已完成的题库原图付费评测。
- 不随意停止或改造 8788；部署重启必须精确限定目标端口与进程。
- 不在目标回复缺失时保存整段反馈历史，也不把反馈专用框选图重复注入普通聊天消息。

## Next Best Step

1. 用下一条真实 A3 多题反馈检查整页框选图质量；若框偏移或缺题，保留反馈编号继续定位。
2. 继续在 8790 验收真实题页，记录多题混排、范围澄清、题号冲突、低清/旋转和小荷载/支座失败。
3. 观察期保留 8896、8897 与 `--disable-auto-crop`；稳定后再决定停止 8897，但不删除其运行状态。

## Important Commands

- `python -m unittest discover -s tests -p 'test_*.py'`
- `powershell -ExecutionPolicy Bypass -File scripts/tiku_admin_watchdog_8795.ps1`
- `powershell -ExecutionPolicy Bypass -File scripts/tiku_agent_watchdog_8790.ps1`
- `python scripts/run_tiku_agent_8790.py --help`
- `python -B scripts/run_tiku_agent_8896.py`
- `python -B scripts/run_tiku_agent_8897.py`
- `python scripts/evaluate_a3_v1_flow.py --images-dir D:\桌面\A3 --output-dir output\a3_v1_flow_desktop_A3_20260820_v1`
- `python scripts/inspect_paddle_layout_candidates.py --help`
- `python scripts/search_by_loads.py --help`
- `python search.py --help`
- `python scripts/evaluate_rerank_matrix.py --dry-run --prompts v1 v4 --providers zhipu qwen --workers 10`
