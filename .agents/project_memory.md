# Project Memory

## Current State

- 项目位于 `F:\cc\7-题库检索`，用于结构力学题库检索与维护。
- 8788 是现有题库飞书机器人；8790 是当前稳定网页主线，生产状态位于 `.tmp_tiku_agent_v2_prod_8790`。8790已启用状态感知安全回答V0、五态工具结果和视觉复筛EXIF方向校准，业务请求仍走Intent V2固定编排。
- 8793 是长期复用的观测与人工评审层，当前临时镜像待真实评审的8794候选提交`455da39`（110文件哈希快照），独立状态位于 `.tmp_review_tiku_prod_8793`，运行身份为 `review-8793-prod`，评审侧栏与历史标签数据均保留；8790已同步同批候选改选修复，其他候选功能保持不变。
- 稳定工作区使用 `codex/mainline-bounded-autonomy-v1`；8794已迁入 `F:\cc\7-题库检索-8794` 独立 worktree 和 `codex/8794-candidate-v1` 分支，源码、端口、Cookie、运行状态和答案输出均与8790/8793/8788隔离。
- 五态工具结果和视觉复筛EXIF方向校准已从8794经8793验收后提升到8790；8794保留并进入影子规划阶段，不同时引入LangGraph。
- LangGraph不与8794同时引入，待有限自主行为稳定后再单独评估迁移。

## Implemented

- CLI、飞书和 Agent 共用题图/荷载识别、章节2–8、主库/字母库路由、粗筛、shape-only视觉复筛和答案定位能力；单章节的读取、结构类型过滤、荷载评分与排序已统一到 `search.scan_chapter_candidates`，入口各自保留写缓存、路径修复、旧候选排除和跨章去重。
- Intent V2 是唯一 Agent 运行时；规则处理明确表达，Qwen处理上下文长尾，代码校验状态、动作权限、参数和编号命名空间。
- 支持单题/多题、章节纠正、候选切换、答案回看、候选否定、非重复续搜、失败恢复及经明确授权的一次严格全局搜索。
- SQLite会话和媒体持久化可跨刷新与重启恢复；Demo支持移动端、裁剪上传、拖放、进度、候选按钮和大图查看。
- 8793直接运行主线镜像并旁路记录意图、授权、工具、状态和最终结果；人工评审记录不会修改业务状态。
- 8793评审侧栏已改为结果导向摘要：意图显示用户想做什么，工具显示可核对的识别或处理结果，状态只显示等待章节/候选/答案等业务阶段；无具体内容的“回答生成”不再进入日常错误归因，但仍保留在折叠技术详情，已有人工勾选或自动异常时仍可回显。`last_intent_action`、原始 phase 和 JSON 也只保留在技术详情。空轨迹或加载异常不再向评审者暴露代码报错原文。
- 候选按钮使用题目版本与确定性候选批次共同校验：上传新题或生成新候选批次后，前后端共同拒绝旧按钮；同一道题、同一候选批次在答案返回后仍可改选其他候选，并形成新的8793评审回合。8790与8793保持独立数据库，8793启动会校验固定运行身份。
- 8794使用独立 worktree、实验分支、专用启动入口、端口8794、候选工作区内的 `.tmp_tiku_agent_v2_candidate_8794` 运行目录和独立Cookie；本地配置只保留题库路径、候选输出目录和`top_k`，密钥来自环境，Session、媒体、incoming、日志和答案输出均不复用8790/8793/8788。
- 多题图的题号归一、章节判定、裁图和Qwen图块筛选已抽至 `tiku_shared.multi_question`；8794 Agent不再依赖飞书运行脚本，8788保留同名兼容入口并调用同一实现。
- 8790与8794均已启用状态感知安全回答V0：先按负面边界排除业务操作、结果/状态依赖、敏感信息和越界请求，其余无需工具的完整对话交给千问自然生成；只有安全对话会获得由状态机生成的六字段脱敏摘要（`phase/chapter/candidate_count/allowed_actions/last_completed_step/waiting_for_user`）。输出按业务阶段约束，并校验状态一致性、候选/答案编造、执行声明、内部字段泄露和状态未变化；不合格时使用分阶段固定兜底。业务请求继续走原固定编排，安全对话与业务信号混合时业务优先。
- 状态感知安全回答真实千问矩阵70条中62条直接通过、8条被安全拦截并兜底；主线全量323项测试通过。状态摘要函数为纯代码转换，不调用模型、不修改AgentState；跨刷新/重启从恢复状态生成摘要，不同会话只读取各自状态。
- Zhipu视觉复筛默认使用`glm-4.6v`和10路候选并发；`config.local.json`可用`zhipu_rerank_model`切换模型，单题与多题链路均已验证。
- 飞书事件去重已改为30分钟TTL、20,000条上限和加锁的缓存，避免长跑时事件ID无界增长；8788现已从主线工作区重新启动并加载当前代码。
- 8790与8794的九个业务工具及候选动作解析器已统一返回 `tool/outcome/code/data/next_state/completed/retryable/error_category`，五态为 `SUCCESS / NO_MATCH / NEEDS_INPUT / PARTIAL / TOOL_ERROR`；固定状态机据此区分正常未命中、补充输入、可用降级和真实故障。
- 8793工具轨迹现记录并显示五态中文名称、稳定原因码、可重试性、下一状态及状态前后变化；旧轨迹没有`outcome`时仍按`ok`兼容，原始错误文本和敏感内容不进入轨迹。最新候选与镜像263项、主线/观测parity 23项和前端语法均通过，8793来源镜像为105文件。
- 8793日常错误归因已去掉重复“执行结果”前缀：路由直接显示主库/字母库，结构分类只显示实际类型，成功粗筛/全局候选数量和不适用结构筛选不再成为可勾选错误步骤；复筛成功显示“复筛完成”，不完整时明确显示已回退粗筛排序。机器五态、原因码和下一状态仍完整保留在技术详情。
- 8793评审用语已进一步面向外部测试者：内部“主库/字母库”改为“按数值荷载题检索/按字母荷载题检索”，单题识别轨迹会保存并显示短小的荷载类型与数值摘要；图片、用户原文、绝对路径和模型原文仍不进入轨迹。
- 同时承担单/多题、荷载和章节线索识别的步骤统一显示为“识别题图”，避免让评审者误以为该步只判断单题或多题。
- 视觉复筛会在发送模型前按EXIF方向校正题图，但仅重编码方向2–8的图片；无旋转标记图片继续发送原字节，题库源文件不改。全库497张中13张需校正，13组同图A/B的高分率由38.5%升至100%；真实组合结构原题复验得0.95，错误钢架候选得0.6。
- 历史 Tk 桌面端已从删除前提交恢复为独立 `legacy_gui.py`，复用当前多 Agent 检索与显示门槛，不影响8790/8793/8794/8788；单题入库改为 plan -> confirm -> backup -> execute，漏存审查仅生成只读计划和报告。
- 普通章节复筛结果启用80%最低可靠门槛：最终分 `>=90%` 展示全部高分候选，`>=80%` 且低于90%只展示最高1道，最高分低于80%返回“未找到可靠相似题”并清空候选；只有复筛正常完成时应用该门槛，超时、失败或不完整仍回退粗筛。8790、8793、8788和8794使用同一规则。
- 唯一候选确认规则已同步8790与8793：等待候选选择且只有1个候选时，“是/是的/对/没错/确认/可以/好的”等整句短肯定直接选择候选1；多候选、已显示答案和复合表达不自动选择。8790主线提交为`90349bc`，8793从该提交重建105文件哈希镜像。
- 8793曾切换到8794候选提交`455da39`完成状态感知安全回答真实评审；同批候选在答案返回后可改选，改选会进入Agent并形成新的评审回合，新题或新候选批次仍拒绝旧按钮。状态感知安全回答现已通过验收并提升8790，8793下一步改为镜像新的8790确定提交。
- 8793不再要求评审者判断“没有结果是否合理”，因为侧栏没有提供足够题库证据；低于80%而不展示候选时，复筛步骤会明确显示进入复筛的候选数、最高最终相似度及“不予展示”的门槛原因。

## In Progress

- 8794下一步实现只记录不执行的影子规划与权限审核；普通业务请求继续走固定快速编排，只有编排未覆盖的长尾才进入后续受限执行。

## Not Implemented

- 尚未实现AI影子规划、计划权限审核、有限自主执行器、一次重规划和调用预算。
- 8793评审记录尚不会自动进入Codex排查上下文；用户说“已经标记”时需主动读取固定评审目录并关联turn/trace。
- 尚未接入LangGraph/checkpoint、新飞书Agent、真实流量统计、访问配额和身份认证。

## Architecture Rules

- 8790保持稳定；8794必须使用独立 worktree、实验分支、端口、Cookie、输出和运行目录，不能复用8790/8793/8788源码工作区或状态。
- 8793是可复用测试外壳，持续镜像当前8790的确定提交；后续8794阶段先独立验证与评审，再按小阶段提升到8790并同步8793镜像。
- 8794不包含8793完整技术评审侧栏；测试、轨迹和逐事件标注继续留在8793。
- 固定编排处理快速路径；AI只在长尾内提出计划，所有步骤由代码权限层审核。
- 工具是否成功、未命中、部分完成或报错由结构化协议和代码确定，AI只选择合法路径并解释结果。
- 每回合最多规划一次、意外结果最多重规划一次、工具数有硬上限、同一失败工具最多重试一次。
- AI不得擅自跨章节、降低阈值、跳过状态、使用旧候选、编造答案或执行写操作。
- 先做影子规划；LangGraph待8794行为稳定后作为独立阶段评估，不与有限自主同时引入。

## Known Risks

- 浏览器历史与服务端状态曾形成两个真相；旧候选已修复，8794仍需以服务端状态和 `allowed_actions` 为权威。
- 评审记录曾因错误运行目录分散；生产8793已固定目录，但自动问题交接闭环尚未实现。
- 有限自主可能增加延迟和成本；必须保留快速路径、单次规划和明确预算，并比较完整任务耗时。
- 题库约800道，合理未命中常见；不能把未命中率直接当Agent错误，也不能为命中降低门槛。
- 真实图片变体、非同文件高分题和外部模型稳定性样本仍不足；开发集不能外推真实泛化能力。
- 2026-07-29一次8794启动命令曾把两枚模型API密钥展开到本机进程命令行和工具输出；相关进程已立即终止并改为安全环境继承。用户明确选择暂不轮换旧密钥，功能可继续，但残余泄露风险仍由用户承担。

## Do Not Do

- 不提交或展示密钥、完整本地配置、题库资产、运行日志和人工评审数据。
- 不回退用户已有修改，不把全局记忆写入项目文件。
- 不在8794开发中改造8788，也不复用8790/8793运行状态。
- 不直接修改live题库；写操作必须确认并备份。
- 不在8794同时引入有限自主和LangGraph。
- 不把自由多工具循环、默认跨章搜索或自动降低阈值当作自主能力。

## Next Best Step

1. 定义影子计划的结构化目标、步骤、参数、停止条件和权限审核协议。
2. 只在8794记录计划与审核结果，不执行AI计划、不改变用户响应。
3. 用8793评审越权、循环、无效计划和额外延迟，再决定是否进入有限执行。

## Important Commands

```powershell
Set-Location -LiteralPath "F:\cc\7-题库检索-8794"
python -B -m unittest discover -s tests -p "test_*.py"
Set-Location -LiteralPath experiments\decision_trace_lab
python -B -m unittest discover -s tests\mainline_parity -p "test_*.py"
python -B "F:\cc\7-题库检索-8794\scripts\run_tiku_agent_8794.py"
Invoke-RestMethod http://127.0.0.1:8790/health
Invoke-RestMethod http://127.0.0.1:8794/health
Invoke-RestMethod http://127.0.0.1:8793/api/observation/source
```
