# Project Memory

## Current State

- 项目位于 `F:\cc\7-题库检索`，用于维护结构力学题库并按题图或荷载检索相似题、返回答案。
- CLI、现有飞书检索/入库/删除、章节 2-8、主库/字母库分流和字母库结构类型筛选可用。
- 独立 `tiku_agent/` 已完成工具层、LLM intent、对话状态和单题/多题 MVP；LangGraph checkpoint 与新飞书机器人尚未接入。
- MVP 后的当前阶段已确定为 Intent V2：先提高多轮指代、上下文动作判断和安全追问的可靠性，不直接重构 LangGraph。
- Agent 与现有飞书继续使用独立入口和运行状态；8790 的生产状态位于 `F:\cc\7-题库检索-intent-v2\.tmp_tiku_agent_v2_prod_8790`，8788 仍使用原飞书状态。
- FastAPI Demo 监听 `127.0.0.1:8790`，题库飞书监听 `127.0.0.1:8788`；两者使用固定 Cloudflare Tunnel，已配置 Windows 登录自启，tunnel 系统服务为自动启动。
- Demo 保留首次访问建会话和刷新恢复题图，并已修复微信裁剪产物缺少文件名/MIME 时的上传链路；HEIC/HEIF 仍不在支持范围。
- 8790 已从 `F:\cc\7-题库检索-candidate-feedback` 隐藏启动并复用原生产状态，自启任务直接调用该工作区 Python 入口；8788 未改动。生产工作区的用户日志继续保留且禁止提交。

## Implemented

- `search.py` 与 `multi_agent_pipeline.py` 已提供粗筛、题图/章节识别、主库/字母库路由、结构类型筛选、并发视觉复筛和答案定位。
- 现有飞书支持单题/多题检索、章节选择、答案返回、入库和候选删除；写操作继续使用计划、确认和备份流程。
- 隔离 Agent 工具、状态和编排已完成；Intent V2 是唯一运行时，严格区分 `question_index` 与 `candidate_rank`，支持章节纠正、多题选题、候选切换、答案回看、专业交流和失败恢复。
- 严格 `global_search` 已部署到8790，只允许在章节未知、有效题图存在且已向用户提供兜底选项后执行；代码复核授权，视觉门槛保持 `rerank_score > 0.95`。
- 全局复筛只对未完成候选进行一次代码控制重试；仍异常则安全失败，不重试已完成或正常低分结果。
- `codex/candidate-feedback` 已实现候选否定、非重复续搜、回看、答案不匹配和按钮命名空间；粗筛统一为“满分全留，否则仅最高1条”，未复筛或回退同样适用；已部署8790，尚未合并。
- SQLite 会话与文件持久化已实现，最后活跃两小时过期；刷新、退出重进和 Demo 重启可恢复会话题图。
- Demo 支持移动端布局、拖放/裁剪上传、候选选择、大图预览、失败重试和真实 Agent 阶段进度；上传按真实图片字节校验并使用 multipart `file`。
- 公网入口已关闭 OpenAPI，并启用 HTTPS 重定向、安全 Cookie、CSP 和基础安全响应头。
- 章节识别先提取 `visible_problem_text`；没有真实题干则保持 `unknown`，不根据结构外形猜章节。
- 赋值符号荷载归一化为无单位 `符号=数值` 并进入主库；纯符号进入字母库。
- 视觉复筛采用 shape-only，只比较主杆件骨架与轮廓；最终粗筛与视觉各占50%，90分以上全显，否则只显示最高分，CLI、飞书和 Agent 共用策略。
- 普通并发复筛最多10个候选；批次不完整时不混用残缺视觉分排序。

## In Progress

- Intent V2 的动作协议、权限矩阵、净化上下文、受约束推理、交流外壳和回归评测已完成；V1 已退役，40条开发集不用于外推泛化准确率。
- 严格全局搜索意图评测为12/12直接成功、错误和未授权执行均为0；7章原题小样本自命中7/7，21个裁边/模糊/截图变体安全复核后命中20/21。样本仍小，非同文件高分题需要人工语义复核。
- 隔离运行时、临时8792和线上8790冒烟已覆盖授权/拒绝全局搜索、单/多/无结果、章节覆盖、候选取答案和跨重启恢复；当前链路可用但缺少真实流量统计。

## Not Implemented

- 尚未引入 LangGraph/checkpoint，也未连接新的飞书机器人或建立真实流量统计；现有8788保持原流程。
- 仍缺少更广的裁图、截图、模糊图和跨章近似题样本，以及对非同文件高分结果的人工语义复核。
- 40条开发回归集不能外推泛化准确率；仍需冻结标签后的盲测/改写集和模型稳定性复跑。
- 复筛并发数、首轮超时和补评上限尚未配置化。
- 缺少 pipeline 级超时回退、真实飞书事件和持久状态集成测试。
- HEIC/HEIF 浏览器解码兼容尚未实现；当前支持 JPEG、PNG、WEBP、GIF、BMP，空 MIME 或 `application/octet-stream` 会先尝试按真实图片字节解码。
- 公网访问次数限制、身份认证和滥用防护尚未启用。

## Architecture Rules

- 新 Agent 必须使用独立入口、配置、凭据、端口、隧道和运行状态；不得为测试 Agent 停止、重启或改造现有飞书机器人。
- 早期 Agent 可以只读 live 题库；入库、删除和修复必须走 `plan -> confirm -> execute` 并备份。
- 章节只能由用户指定/确认，或由题干文字证据自动确定；纯结构图不能跨章节猜测。唯一例外是 Intent V2 在章节未知后先提供选项，并经用户明确同意执行一次严格只读全局搜索。
- 荷载类型只用 `集中`、`均布`、`弯矩`；`raw` 保留原始标注并做单位归一化。
- 主库保存数值荷载和已赋值符号题，字母库保存未赋值字母题；只有字母库图片检索使用结构类型筛选。
- 复筛只处理粗筛候选，不改变候选池；未完成批次不能混合视觉分排序。
- Intent V2 每个用户回合最多执行一个高层动作；LLM 负责理解，代码负责状态权限、参数范围和禁止动作校验，不开放自由多工具循环。
- Intent 上下文只提供完成判断所需的会话摘要，不暴露本地绝对路径、凭据、完整模型原始输出或无关题库数据。
- `greeting`、`small_talk`、`capability_help`、`out_of_scope` 和 `clarification` 不调用题库工具、不修改搜题业务状态，也不把 Agent 扩展成通用聊天机器人。
- 模糊编号跟随最近活跃命名空间，本回合明确名词优先；活跃编号越界时追问，不静默切换到另一命名空间。
- 发图前章节预设只作用于下一张有效单题图片；多题图片必须确认具体题号，不能把预设章节批量套用。
- `global_search` 只允许作为章节未知后的显式授权兜底；代码必须核验已提供选项和当前题图，不能演变为自由跨章节搜索。
- 2026-07-14 完成 V1 退役；历史 V1 指标只保留作背景，不再提供 V1 运行、测试或回退入口。

## Known Risks

- 微信/荣耀真机裁剪产物仍未采样；Chromium 模拟无后缀和通用 MIME 已通过，真机内存、EXIF 与 HEIC 仍需实测。
- Intent 获得的会话上下文有限，“另一个/刚才那个/剩下那道”等指代仍容易误判。
- 73条发布集规则层64条直达、9条安全追问且Qwen此前9/9命中；20条结果反馈连续3轮20/20，本地回归193/193，但均为开发集。
- 隔离完整链路中单题、多题编号、非重复续搜、耗尽停止和答案反馈通过；全局搜索否定授权漏洞已由执行前统一校验修复，专项13/13、错误执行和未授权调用均为0。明确授权后的真实全局复筛仍可能因候选未完成安全进入ERROR。
- 同粗筛分的超时候选补评顺序可能受线程完成顺序影响，尚无确定性次序。
- `search.py` 和现有飞书脚本体量较大，章节及中文序号解析存在行为漂移风险。
- 单测以 mock/纯函数为主，外部模型、飞书事件与持久状态覆盖不足。
- 公网 Demo 依赖本机和 Cloudflare Tunnel；当前没有访问配额，可能产生模型费用。
- 当前8790未包含生产提交 `9d09376` 的最新矮屏修复，用户决定由后续页面任务补回。
- 依赖未完整锁版；本地配置会覆盖代码默认值，调整参数时须检查有效配置。
- Windows 验证优先使用 `python -B`，避免生成并锁定 `__pycache__`。

## Do Not Do

- 不提交、展示或写入 API key、token、密码和完整本地配置。
- 不回退用户已有改动，不把全局记忆写入项目文件。
- 不复用现有飞书机器人的运行状态开发 Agent，也不随意重启现有飞书服务。
- 不直接修改 live 题库；写操作必须确认并备份。
- 不让 LLM 直接用多题 bbox 复筛；继续使用 OpenCV 图块流程。
- 不让复筛展示规则改变粗筛候选池。
- 不提交 `data/feishu_chapter_failure_log.jsonl` 等运行日志。
- 未拿到真实 HEIC/HEIF 样本前，不扩展全格式服务端转码或引入 HEIF 依赖；当前客户端只规范化浏览器能解码的支持格式。

## Next Best Step

1. 观察8790真实使用；后续页面任务补回矮屏修复后，复跑全量与390×520长对话冒烟。
2. 扩充跨章近似题和图片变体样本，人工区分文件精确率与语义正确率，并观察真实无结果率、续搜次数和额外回合数。
3. 后续只维护 V2；暂不改单双题识别调用结构，不动现有飞书和 LangGraph 边界。

## Important Commands

```powershell
python -B -m unittest discover -s tests -p "test_*.py"
python -B -m unittest tests.test_tiku_agent_action_decision_v2
python -B -m unittest tests.test_tiku_agent_action_permissions_v2
python -B -m unittest tests.test_tiku_agent_agent tests.test_tiku_agent_session_runtime tests.test_tiku_agent_fastapi_demo
python -B scripts/smoke_test.py
python -B scripts/search_by_loads.py --help
python -B search.py --help
python -B -m uvicorn tiku_agent.fastapi_demo:create_app --factory --host 127.0.0.1 --port 8790
Invoke-RestMethod http://127.0.0.1:8790/health
```
