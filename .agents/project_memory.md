# Project Memory

## Current State

- 项目是结构力学题库检索器；CLI、飞书机器人和 Agent 共用核心检索逻辑，但运行状态彼此隔离。
- 8788 是飞书搜题工作流；8790 是邀请制网页主线；8793 是观察/评审镜像；8794 是隔离 Agent；8795 是独立管理员后台。
- 8790 已切换为读取 8795 共享控制库；原静态邀请码配置仅用于灾备回退。正式 8795 在 `http://127.0.0.1:8795` 保持健康，并由 Windows 登录任务 `Tiku Admin 8795` 启动独立看门狗保活；控制库当前有 1 条启用、13 条已归档邀请码。
- 8795 第一版已实现管理员登录、邀请码生命周期与动态额度、今日搜题/费用概览、单码用量、反馈筛选和完整对话详情；邀请码与反馈均支持归档、恢复和受保护永久删除。
- 反馈案例保存用户可见对话和相关题目/候选/答案图片，默认 30 天后清理；不保存邀请码明文、隐藏 Prompt、内部推理、密钥或本地路径。
- 8790 通过 `--control-db` 动态读取邀请码状态与全站/单码额度；停用或重置会递增认证版本并使旧登录失效。
- 8790 保持 1 个活动任务、2 个排队任务、最长等待 55 秒；费用按 Asia/Shanghai 自然日，金额是项目估算，不等同供应商账单实扣。
- 当前题库共 280 行，只有 24 行满足 >20，均属“4力法—钢架—均布 q”组；真实覆盖仍窄。
- 8795 将邀请码备注统一显示为“用户”；旧码显示“未命名用户”、内部编号和“旧邀请码不可查看”。
- 完整 449 项测试通过；8795 已完成桌面/390px 窄屏、反馈日期统一空态与间距、真实章节筛选、反馈编号/详情/归档恢复、已归档邀请码操作和最近操作折叠浏览器验收，浏览器错误和警告为 0。8790、8795 均保持健康。

## Implemented

- `dimensions.py`、`structure_dimensions.py`：尺寸解析、归一化、结构约束、缓存和 Qwen `qwen3.7-plus` 识别。
- `multi_agent_pipeline.py`、`search.py`、`tiku_agent/tools.py`、`tiku_agent/agent.py`：共享荷载检索、尺寸复筛和视觉重排。
- 全尺寸和单边尺寸不区分横竖方向；缺边不按图形比例补齐。拱不参与复筛。
- 字母库全局搜索仅在章节判断失败且用户明确授权后执行；跨第 2–8 章按结构类型收集并去重。
- 支持章节问题走零模型、零业务工具的代码事实回答；混有搜题指令时仍走 Intent V2。
- 8790 具备有界执行、同会话串行、过期会话清理、媒体 no-store、安全响应头及忙碌/额度中文错误。
- 手机网页会在上传前将大图自适应压到约 1MB；Qwen 与智谱共享模型输入编码器还覆盖 8788 飞书、8790、8793、8794 和图片 CLI，绕过网页的大图也会在内存中限制到最长边 2560px、必要时 2048px，小图和源文件保持不变。
- `tiku_shared/model_costs.py`：费用台账按稳定邀请码身份记录搜题次数和估算费用，兼容迁移旧 SQLite schema。
- `tiku_agent/feedback_store.py`：支持反馈去重更新/取消、稳定 `FB-日期-短码` 编号、用户侧搜题耗时、可见对话与媒体案例、章节与检索键、独立归档状态、处理状态/备注及到期清理。
- `tiku_admin/control_store.py`：管理员密码和 8790 邀请码认证只使用哈希；8795 另用 AES-GCM 密钥加密新建/重置邀请码，支持脱敏预览、审计复制、生命周期、额度、设置及旧配置预检/导入。
- `tiku_admin/app.py`、`auth.py`、`reporting.py`、`invite_vault.py` 和 `web/`：提供独立 8795 FastAPI 后台；列表不返回邀请码明文，管理员复制时通过 CSRF 保护的 POST 接口临时解密并写入审计，响应禁止缓存。
- 8795 概览显示今日搜题、估算总费用、启用邀请码、待处理点踩和每码费用；反馈可按日期、评价、用户、真实非空章节和处理状态筛选并跳转完整对话。“全部章节”包含章节为空的记录，反馈原因继续在列表/详情展示但不作为筛选项。反馈按提交时间倒序，每页 50 条且超过一页才显示翻页；归档反馈只保留取消归档和永久删除，恢复后才允许查看详情。
- 已归档邀请码只显示取消归档和永久删除，不再显示编辑；有费用或反馈历史时永久删除会被拒绝。反馈默认排除已归档，可显示已归档后恢复或永久删除；设置页审计记录永久保留并按每页 10 条服务端分页。
- `scripts/run_tiku_admin.py`、`manage_tiku_admin.py`、`tiku_admin_watchdog_8795.ps1`：提供启动和独立保活；本机已注册登录触发、隐藏运行、失败每分钟重启且忽略重复实例的 `Tiku Admin 8795` 计划任务。旧邀请码导入默认只预检，必须显式 `--apply-import` 才写入。
- `scripts/run_tiku_agent_demo.py` 与 8790 看门狗支持可选 `--control-db` / `-ControlDb`；控制库模式拒绝同时使用旧邀请码配置或静态额度参数。
- `scripts/switch_tiku_agent_8790_control.ps1` 默认只读预检并唯一定位当前看门狗；预检同时要求旧配置 ID、哈希、状态和认证版本一致，防止后台验收污染正式切换或使旧 Cookie 失效。显式 `-Apply` 才备份、切换、联动验收，失败自动恢复旧哈希配置。

## In Progress

- 当前 2 条反馈均为活动状态并已迁移稳定编号；完整可见对话链路已有真实数据实证。用户侧搜题耗时从新版本开始记录，旧反馈无法可靠还原并显示“暂无数据”。
- 本地迁移已完成；创建公网 `admin` 子域名前仍需配置 Cloudflare Access/WAF，再邀请 5–10 名真实用户内测。

## Not Implemented

- `admin-tiku.cysbuting.cn` 的 Cloudflare Tunnel Published application 路由已创建；Cloudflare Access/WAF 和登录限速外层保护尚待确认。
- 桁架高度几何计算未实现；模型只抄录明确标注。
- 视觉重排同分候选缺少稳定的原粗排名次作为次级排序键。
- 真实触发覆盖尚不能证明其他章节、梁、桁架或单边尺寸效果相同。
- 云服务器和云模型扩容尚未进行；当前电脑仍是 8790 服务器。

## Architecture Rules

- 8795 独立运行在 8790 之外，管理员认证、Cookie、运行目录和控制数据不得与用户邀请码会话混用。
- 费用按稳定邀请码身份归属，不按可刷新或清除的临时会话 Cookie 归属。
- 保留单邀请码额度与全站保险上限；调整邀请码数量或单码额度时同步核算理论总暴露。
- 8790 认证始终只读取邀请码哈希；8795 可单独加密保存新建/重置邀请码的可恢复副本，列表只显示脱敏值，完整值仅经管理员会话、CSRF 和审计保护的复制接口返回。
- 旧邀请码只有哈希，无法还原；必须重置后才可复制。`control.sqlite3` 与邀请码加密密钥必须一起备份、保护和迁移。
- 迁移 8790 前先只读预检并显式合并原哈希邀请码配置；冲突时禁止写入，`--control-db` 与 `--invite-config`、静态费用上限不得混用。
- 灾备迁移或重切前必须核对旧配置与控制库的 ID、哈希、状态和认证版本；普通幂等导入不能当作线上状态或 Cookie 兼容证明。
- 预算在任务准入前检查、完成后落账；当前全局单任务并发用于防止并发穿透额度。
- 费用是依据版本化目录价格和 usage 的估算；缺失 usage 或价格时不能宣称等同供应商账单。
- 反馈只保留最小必要可见上下文；案例到期后清理对话和媒体，评分、原因、反馈编号、归档与处理状态继续保留。搜题耗时定义为上传题图至首批候选在页面可见的用户等待时间，不以模型费用任务起止时间代替。
- 修改检索、储存或索引逻辑时同步检查 CLI、飞书、Agent 和项目 Skill 文档。
- 题库写操作继续 plan → confirm → backup → execute；8790、8793、8794、8795 与 8788 的状态和端口保持隔离。

## Known Risks

- 邀请码被转发会共享同一 3 元额度；签名 Cookie 防伪造但不能阻止持码人主动共享。
- 费用完成后入账，因此最后一个在途任务可能使单码或全站金额略高于阈值。
- usage 缺失、价格目录缺项或供应商规则变化会造成估算偏差；30 元不是支付平台硬限额。
- `admin-tiku.cysbuting.cn` 已接入 Tunnel，但 Access/WAF 尚未确认；完成外层保护前仍可能只有应用层管理员门禁。
- Qwen 冷调用有长尾；候选下降不保证首次总耗时一定下降。
- 2026-07-29 曾有旧 API 密钥出现在进程命令行和工具输出；旧进程已终止，用户当时选择暂不轮换。

## Do Not Do

- 不读取、提交或在工具输出、日志、项目文档中展示 API key、Tunnel token、邀请码明文、管理员密码、私有发放清单和本地敏感配置；管理员页面的受控复制功能除外。
- 不把管理员认证并入用户邀请码会话，不把 8795 部署进 8790 进程。
- 不因后台记录的 ID/哈希一致就假定旧邀请码可用；灾备切换还必须核对状态并验证登录和动态撤销。
- 不把邀请码身份改回会话 Cookie，也不删除全站保险上限只保留单码额度。
- 不跨章节搜索，不绕过项目脚本的识别、过滤和排序。
- 不在未授权时把图片发给外部模型，不重复已完成的题库原图付费评测。
- 不随意停止或改造 8788；部署重启必须精确限定目标端口与进程。

## Next Best Step

1. 下一条真实反馈出现后，在 8795 核对完整对话、题图、本题费用和非“暂无数据”的搜题耗时；旧反馈无法补出历史耗时。
2. 为 `admin-tiku.cysbuting.cn` 启用并无痕验证 Cloudflare Access/WAF，只允许管理员邮箱访问。
3. 公网保护完成后邀请 5–10 名真实用户内测，再按真实反馈决定第二版功能。

## Important Commands

- `python -m unittest discover -s tests`
- `python scripts/run_tiku_admin.py --help`
- `python scripts/manage_tiku_admin.py --control-db .tmp_tiku_admin_8795/control.sqlite3 --import-invites <原哈希配置> --require-status-match`（灾备切换前严格只读预检）
- 普通导入确认后添加 `--apply-import`（显式补入缺失记录，不覆盖后台状态）
- `powershell -ExecutionPolicy Bypass -File scripts/switch_tiku_agent_8790_control.ps1`（灾备重切前只读预检）
- `powershell -ExecutionPolicy Bypass -File scripts/tiku_admin_watchdog_8795.ps1`
- `python scripts/run_tiku_agent_demo.py --help`
- `python scripts/manage_tiku_invites.py --help`
- `python scripts/search_by_loads.py --help`
- `python search.py --help`
