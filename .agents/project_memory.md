# Project Memory

## Current State

- 项目位于 `F:\cc\7-题库检索`，用于结构力学题库检索与维护。
- 8788 是现有题库飞书机器人；8790 是当前稳定网页主线，生产状态位于 `.tmp_tiku_agent_v2_prod_8790`。8790已启用安全回答V0、五态工具结果和视觉复筛EXIF方向校准，业务请求仍走Intent V2固定编排。
- 8793 是长期复用的观测与人工评审层，镜像当前8790的105文件哈希快照，独立状态位于 `.tmp_review_tiku_prod_8793`，运行身份为 `review-8793-prod`，评审侧栏与标签数据均保留。
- 稳定工作区使用 `codex/mainline-bounded-autonomy-v1`；8794已迁入 `F:\cc\7-题库检索-8794` 独立 worktree 和 `codex/8794-candidate-v1` 分支，源码、端口、Cookie、运行状态和答案输出均与8790/8793/8788隔离。
- 五态工具结果和视觉复筛EXIF方向校准已从8794经8793验收后提升到8790；8794候选服务当前运行在独立端口8794，状态感知安全回答已在候选分支完成实现与复核修正，仍需进入8793真实评审后再决定是否提升8790。
- LangGraph不与8794同时引入，待有限自主行为稳定后再单独评估迁移。

## Implemented

- CLI、飞书和 Agent 共用题图/荷载识别、章节2–8、主库/字母库路由、粗筛、视觉复筛和答案定位；支持单/多题、章节纠正、候选切换、答案回看、续搜、失败恢复及一次明确授权的全局搜索。
- Intent V2 是唯一运行时：规则处理明确表达，Qwen处理上下文长尾，代码校验状态、权限、参数和编号命名空间；SQLite会话与媒体可跨刷新/重启恢复。
- 8793旁路记录主线的意图、授权、工具、状态与结果，评审不改业务状态；侧栏默认展示结果导向摘要，技术字段折叠，启动时校验固定身份。
- 题目版本和候选批次保护会拒绝新题后的旧按钮；同一批候选在答案返回后仍可改选其他候选。8790/8793已部署并保持独立数据库。
- 8794的worktree、分支、端口8794、Cookie、运行目录、媒体、日志和答案输出均与8790/8793/8788隔离；多题图处理已抽到`tiku_shared.multi_question`。
- 8790/8794已启用安全回答V0；8794进一步实现严格六字段`SafeConversationContext`、中文动作语义、独立校验事实、状态矛盾/虚构执行/敏感信息校验和安全兜底。业务操作仍由固定状态机优先处理。
- 唯一候选时的整句短肯定由固定Intent选择候选1；多个候选、答案已显示或复合表达不自动选择。112条状态护栏和真实Qwen矩阵已覆盖主要阶段。
- 九个业务工具统一使用`SUCCESS/NO_MATCH/NEEDS_INPUT/PARTIAL/TOOL_ERROR`结果协议，固定状态机按协议转移；视觉复筛使用`glm-4.6v`、10路并发并校正EXIF方向2–8。
- 8794影子Planner只记录不执行；状态权限层和纯代码语义闸门覆盖11个动作。35条真实入口金标准三轮99/105，固定路径误入Planner=0、可见差异=0、禁止动作实际放行=0。
- 40条平衡准入压力集证明：顺序/条件请求真实进入Planner为0，原子误入为0；顺序组6/10类可稳定完整规划，条件组30轮有12轮直接调用工具，当前不能开放条件规划。
- 准入评估已新增Agent专用暂定权重：原子70%、含糊/暂不支持20%、顺序8%、条件2%。旧120轮按此计算入口契约分90%，但顺序/条件未准入且条件专项触发安全硬门槛，`release_ready=false`。该权重明确不是历史语言频率。
- 8790/8793脱敏日志可统计动作与阶段：8790为188轮/51会话，8793为119轮/24会话；服务端按隐私设计不保存用户原话，因此不能从历史数据还原多动作、条件、纠正等语言类别占比。

## In Progress

- Stage 5 准入基线已完成。下一步优先修复条件表达保护：固定Intent和语义闸门必须区分“当前授权”与“条件成立后才允许”；修复前不扩大条件请求Planner准入。顺序请求的首批影子准入候选可在条件保护后单独讨论。

## Not Implemented

- 尚未实现有限自主执行器、逐步骤重新审核、一次重规划和真实执行调用预算；影子规划仍只记录，不执行工具。
- 8793评审记录尚不会自动进入Codex排查上下文；用户说“已经标记”时需主动读取固定评审目录并关联turn/trace。
- 尚未接入LangGraph/checkpoint、新飞书Agent、隐私保护的语言类别统计、访问配额和身份认证。

## Architecture Rules

- 状态感知安全回答沿用负面业务边界：选择候选、继续搜索、查答案等操作不能进入安全回答；允许给模型看的状态仅限`phase/chapter/candidate_count/allowed_actions/last_completed_step/waiting_for`等脱敏字段。
- 模型可在严格六字段白名单内自由组织语言；题图与答案存在性只进入代码校验事实，不进入模型上下文。代码必须校验无虚构执行、无编造结果、无敏感信息且状态前后不变；失败时沿用通用安全兜底，完整阶段引导由既有业务状态机负责。
- 8790保持稳定；8794必须使用独立 worktree、实验分支、端口、Cookie、输出和运行目录，不能复用8790/8793/8788源码工作区或状态。
- 8793是可复用测试外壳，持续镜像当前8790的确定提交；后续8794阶段先独立验证与评审，再按小阶段提升到8790并同步8793镜像。
- 8794不包含8793完整技术评审侧栏；测试、轨迹和逐事件标注继续留在8793。
- 固定编排处理快速路径；AI只在长尾内提出计划，所有步骤由代码权限层审核。
- 工具是否成功、未命中、部分完成或报错由结构化协议和代码确定，AI只选择合法路径并解释结果。
- 每回合最多规划一次、意外结果最多重规划一次、工具数有硬上限、同一失败工具最多重试一次。
- AI不得擅自跨章节、降低阈值、跳过状态、使用旧候选、编造答案或执行写操作。
- 先做影子规划；LangGraph待8794行为稳定后作为独立阶段评估，不与有限自主同时引入。

## Known Risks

- 浏览器历史与服务端状态曾形成两个真相；旧候选已修复，8794仍需以服务端状态和 `allowed_actions` 为权威。
- 评审记录曾因错误运行目录分散；生产8793已固定目录，但自动问题交接闭环尚未实现。
- 有限自主可能增加延迟和成本；必须保留快速路径、单次规划和明确预算，并比较完整任务耗时。
- 题库约800道，合理未命中常见；不能把未命中率直接当Agent错误，也不能为命中降低门槛。
- 真实图片变体、非同文件高分题和外部模型稳定性样本仍不足；开发集不能外推真实泛化能力。
- 2026-07-29首次重启8794时，启动字符串错误地把两枚用户级模型API密钥展开到进程命令行；相关父子进程已立即停止，现改为Python进程继承环境且命令行不含密钥值。两枚密钥仍必须轮换后才视为处置闭环。

## Do Not Do

- 不提交或展示密钥、完整本地配置、题库资产、运行日志和人工评审数据。
- 不回退用户已有修改，不把全局记忆写入项目文件。
- 不在8794开发中改造8788，也不复用8790/8793运行状态。
- 不直接修改live题库；写操作必须确认并备份。
- 不在8794同时引入有限自主和LangGraph。
- 不把自由多工具循环、默认跨章搜索或自动降低阈值当作自主能力。

## Next Best Step

1. 先为“如果/若/否则/前提是”等条件表达建立固定Intent和语义授权专项金标准，禁止条件未满足时直接执行业务动作；保留明确无条件指令原行为。
2. 条件保护通过后，只选择6类已连续三轮完整规划的顺序请求做第一批影子准入候选，扩充留出样本后再实现；单动作快速路径仍不得调用Planner。
3. 再处理重复选题、排除后续搜、未来候选预选等丢步骤/需延后的计划表达；之后才讨论关键工具拆分、`inspect_question`和8793评审。有限执行与LangGraph继续后置。

## Important Commands

```powershell
Set-Location -LiteralPath "F:\cc\7-题库检索-8794"
python -B -m unittest discover -s tests -p "test_*.py"
Set-Location -LiteralPath experiments\decision_trace_lab
python -B -m unittest discover -s tests\mainline_parity -p "test_*.py"
python -B "F:\cc\7-题库检索-8794\scripts\run_tiku_agent_8794.py"
Invoke-RestMethod http://127.0.0.1:8790/health
Invoke-RestMethod http://127.0.0.1:8794/health
Invoke-RestMethod http://127.0.0.1:8793/api/observation/source
```
