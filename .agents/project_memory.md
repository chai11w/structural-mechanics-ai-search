# Project Memory

## 当前状态

- 项目是结构力学题库检索器；CLI、飞书机器人和新 Agent 共用核心检索逻辑，但运行状态必须彼此隔离。
- 8788 是现有飞书题库机器人且不得随意停改；8790 是稳定网页主线；8793 是独立评审镜像；8794 位于隔离 worktree，有限自主化暂缓，详见 `.agents/roadmap.md`。
- 尺寸复筛已实现并同步到 CLI、飞书和 Agent，生产默认关闭，尚未重启或切换线上服务。
- 触发条件：题目为符号尺寸、已知参与结构类型、荷载满分候选严格大于 20；沿用前序结构类型，不重复识别。
- 复筛只调用一次 Qwen V5.2。完整尺寸精确不符会删除；单边命中只前排；单边缺失或不符仍保留；异常、冲突、结果未验证或将删空时回退原候选。
- 拱不参与；梁的完整尺寸必须含 0；钢架、桁架和组合结构的完整尺寸不得含 0。尺寸缓存键含图片、模型、提示词版本和结构类型。
- 当前库共 280 行，只有 24 行满足 `>20`，且全部属于“4力法—钢架—均布 q”一组；钢架单边组最多 20，梁最多 8，桁架最多 13。
- 相关单元测试共 414 项通过，核心 CLI 帮助检查通过；最近代码和文档已提交并推送。

## 已实现内容

- `dimensions.py`：尺寸解析、归一化、匹配和复筛规则。
- `structure_dimensions.py`：V5.2 识别、结构约束、独立缓存。
- 尺寸模型确定为 `qwen3.7-plus`；3.6-flash 全库速度较快但效果被用户否决。V5.2 只转录显式标注，平行尺寸链取能包住主骨架的较长外侧链。
- `multi_agent_pipeline.py`、`search.py`、`tiku_agent/tools.py`、`tiku_agent/agent.py`：接入共享复筛流程。
- `dimension_filter_enabled` 默认 `false`，便于小流量开启和立即回退。
- 全尺寸和单边尺寸均不区分横竖方向，旋转不影响匹配；不得按图形比例自行补齐缺失边。
- 题库中钢架/桁架完整尺寸的 `×0` 已清理；拱的陈旧尺寸 `2静定结构/4拱/1内力/题目/8.jpg = 16×4` 已清空。
- 题库备份：`F:\cc\_backups\7-题库检索\2026-08-09\before_single_side_dimension_20260809_130425` 和 `before_arch_dimension_cleanup_20260809_133949`。
- 视觉重排保留原粗排候选和成本台账；尺寸复筛发生在荷载初筛之后、视觉重排之前。

## 已验证结果

### 用户样题 17.jpg

- 路径：`D:\桌面\答疑、帮做\结构力学\帮做\4力法\2钢架\1单未知量\题目1\1L\17.jpg`。
- Qwen 识别：钢架、均布荷载 q、4力法，尺寸 `L×L`；荷载识别约 3.433 秒。单独结构验证约 2.270 秒，但真实流程走题干文字快速路径，不会重复调用。
- 荷载满分候选 24 个；尺寸冷调用 2.709 秒，24→9；命中缓存后中位约 0.025 秒。原题在两种流程中均保留，最终分数 0.975。
- 旧流程智谱重排 24 个候选约 8.244 秒；新流程重排 9 个约 5.107 秒，节省约 3.137 秒。
- 首次冷调用计入尺寸识别后，完整流程仅净省约 0.45 秒；缓存命中时核心流程约省 3.14 秒。相同最终分数的显示顺序仍可能变化。

### 自动题库金丝雀

- 临时目录：`.tmp_structure_dimension_eval/bank_canary_suite_20260809/`，被 Git 忽略。
- 触发样本 `4力法/题目/7.jpg`：库和模型均为 `1.5L×L`，24→1，原题保留；冷 3.400 秒，热约 0.024 秒。
- 触发样本 `4力法/2钢架/2多未知量/题目aa/3中铰/4.jpg`：均为 `8L×7L`，24→1；冷 3.299 秒，热约 0.023 秒。
- 触发样本 `4力法/3对称性/题目1/38.jpg`：均为 `15×10`，24→1；冷 6.326 秒，热约 0.025 秒。
- 不触发样本：单边钢架 `5位移法/2钢架/题目/4.jpg` 为 20 个；梁 `4力法/3对称性/题目1/2梁/41.jpg` 为 8 个；桁架 `2静定结构/题目/8.jpg` 为 13 个。三者均未调用尺寸模型且候选不变。

## 待办与授权边界

- 还需对两张触发样本各做一次旧 24 候选与新 1 候选的智谱重排对比，共 4 轮、约 50 次候选视觉评估：
  - `4力法/题目/7.jpg`
  - `4力法/3对称性/题目1/38.jpg`
- 脚本已准备：`.tmp_structure_dimension_eval/bank_canary_suite_20260809/run_rerank_canaries.py`，但尚未执行。
- 之前用户只明确授权了 17.jpg 的外部调用；新对话必须先说明会把上述查询图和候选图发给智谱并产生费用，取得本批次明确授权后才能运行。
- 若结果可接受，建议先仅在新 Agent 的 8790 入口开启并观察，再考虑 8788；不要直接全量上线。

## 尚未实现

- 桁架高度的几何计算未实现；V5.2 目前只抄录明确标注，不能宣称已解决计算高度。
- 视觉重排同分候选缺少稳定的原粗排名次作为次级排序键。
- 当前真实触发覆盖只有一个钢架 q 分组，尚不能证明对其他章节、梁、桁架或单边尺寸同样有效。
- 自主检索跨阶段计划见 `.agents/roadmap.md`，与本次尺寸复筛上线决策分开。

## 风险与约束

- Qwen 冷调用有长尾，`15×10` 样本曾用 6.326 秒；候选虽由 24→1，首次总时长仍可能不降反升。
- 题库包含历史人工补全错误，缺边时只能保留单边，禁止凭视觉比例补另一边。
- 智谱候选同分时顺序可能不稳定；判断效果应先看原题是否保留、正确答案是否进入前列，再看顺序。
- 不读取或提交密钥、题库资产、模型日志和临时缓存；任何题库写操作继续执行 plan → confirm → backup → execute。
- 2026-07-29 曾有旧 API 密钥出现在本机进程命令行和工具输出；相关进程已终止且启动方式已修正，用户当时选择暂不轮换，残余风险仍存在。
- 不跨章节搜索，不绕过项目脚本的识别和排序逻辑，不在未授权时把图片发送给外部模型。

## 新对话下一步

1. 先询问用户是否明确授权上述两张图的 4 轮智谱调用及相应费用。
2. 获得授权后运行 `python .tmp_structure_dimension_eval/bank_canary_suite_20260809/run_rerank_canaries.py`，记录旧/新命中、排名和耗时。
3. 根据结果决定是否只在 8790 开启；生产仍保持关闭，并保留配置回退。
4. 将同分稳定排序作为独立小改动评估，不与本次上线开关混在一起。

## 常用验证

- `python scripts/search_by_loads.py --help`
- `python search.py --help`
- `python -m unittest discover -s tests`
- `git status --short --branch`
